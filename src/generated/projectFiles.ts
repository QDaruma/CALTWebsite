// AUTO-GENERATED by scripts/bundle-tasks.mjs. Do not edit by hand.
// Run `npm run bundle` (regenerates from ./task-sources) to refresh.
/* eslint-disable */

export const BUNDLED_TASKS: string[] = ["border_basis","eigvec_3x3","gf17_addition","groebner_basis","integer_factorization","parity","polynomial_addition"];

export const TASK_FILES: Record<string, Record<string, string>> = {"border_basis":{"border_basis/README.md":"# border_basis\n\n**Task**: Given polynomial generators of a zero-dimensional ideal I ⊂ K[x, y, z],\npredict the border basis of I.\n\n## The math\n\n### What is a border basis?\n\nLet I ⊂ K[x1,…,xn] be a **zero-dimensional ideal** (the variety V(I) is a finite set\nof points). A **border basis** of I is an alternative to a Gröbner basis that is:\n\n- Uniquely determined by a chosen **order ideal** O(B) (a set of monomials\n  closed under divisibility that indexes the quotient ring K[x]/I)\n- Often **more numerically stable** than a Gröbner basis\n- Related to the **BM algorithm** (Mourrain / Cerlienco–Mureddu)\n\n**Formal definition**:\nLet O = {t1, …, tμ} be an order ideal for I of size μ = dimK(K[x]/I).\nThe **border** ∂O = {x·ti : ti ∈ O, x·ti ∉ O} of O consists of the monomials\njust outside O. The border basis B = {b_τ : τ ∈ ∂O} satisfies:\n\n    b_τ = τ − Σ_{ti ∈ O} c_{τ,i} · ti\n\ni.e., each border monomial τ reduces to a linear combination of O-monomials.\n\n### Key difference from Gröbner basis\n\n| | Gröbner basis | Border basis |\n|---|---|---|\n| Ideal type | Any ideal | Zero-dimensional ideals |\n| Monomial order | Required (lex, grevlex, …) | Not required |\n| Numerical stability | Often poor | Often better |\n| Uniqueness | Up to choice of order | Up to choice of order ideal |\n\n### This task\n\n- **Input**: polynomial generators g1, …, gk over GF(p) (a finite field)\n- **Output**: the elements of the border basis of ⟨g1,…,gk⟩\n\nWorking over GF(p) ensures the ideal is automatically zero-dimensional\n(the variety V(I) ⊆ GF(p)^n is a finite set of at most p^n points).\n\n## Status\n\n✅ **Real border basis algorithm implemented** in `core/algorithm.py`. It is a\nverbatim port of Algorithm 4.1 (BBasis) from:\n\n> Kera, Pelleriti, Ishihara, Zimmer, Pokutta,\n> *Computational Algebra with Attention: Transformer Oracles for Border Basis Algorithms*,\n> arXiv:2505.23696, 2025.\n> Code: <https://github.com/HiroshiKERA/OracleBorderBasis>\n\nThe algorithm:\n1. Computes the **L-stable span** of F up to a sufficient degree (Algorithm 2.13).\n2. Finds the **optimal order ideal** O via a Mixed Integer Linear Program\n   (Lemma 3.9, solved with `scipy.optimize.milp`).\n3. Applies a **basis transformation** (Algorithm 4.3) to extract the border basis G.\n\nFast Gaussian elimination uses a `SortedList` from `sortedcontainers` for\nO(log n) reducer lookup.\n\n## Experiments\n\n| Experiment | What varies |\n|---|---|\n| `toy/` | Small field GF(7), 2 variables, small degree |\n| `finite_field/` | Compare GF(7), GF(11), GF(17), GF(31) |\n| `ablation/` | Number of generators, degree bounds |\n\n## Quick start (once TODO is filled)\n\n```bash\ncd border_basis/experiments/toy/scripts\npython generate.py\npython train.py\npython evaluate.py\n```\n\n## User post-processing hooks (optional)\n\nTwo opt-in slots inject custom `(input, target)` transformations between the\nload chain and tokenization. Identical mechanism to `groebner_basis`, see\n[groebner_basis/README.md](../groebner_basis/README.md#user-post-processing-hooks-optional).\n\nPer-task slot for this task: `border_basis/core/postprocessor.py` (copy the\n`.example` file and edit).\n\n## Tokenization format (raw vs C/E expanded)\n\nSame mechanism as `groebner_basis`. Two lexer configs are provided and the format is\nauto-detected from `data.lexer_config` in your `train.yaml`:\n\n```yaml\ndata:\n  lexer_config: ../configs/lexer.yaml           # raw  (default, ISSAC '26 paper)\n  # lexer_config: ../configs/lexer_expanded.yaml  # C/E expanded (smaller vocab)\n```\n\n- **raw** — `\"x^2 + y\"` → `[\"x\", \"^\", \"2\", \"+\", \"y\"]`. Default.\n- **expanded** — each term as `C<coef> E<exp_x> E<exp_y>`. Fixed-size vocab.\n\nWhen `expanded` is detected, `core/train.py` wires `ExpandedFormLoadPreprocessor` into\nthe load chain and **requires** `--data_config_path` (it needs the source ring from\n`data.yaml`). See [groebner_basis/README.md](../groebner_basis/README.md#tokenization-format-raw-vs-ce-expanded)\nfor details and trade-offs (they are identical for both tasks).\n","border_basis/core/__init__.py":"from .generator import BorderBasisGenerator\nfrom .algorithm import BorderBasisCalculator\nfrom .formatter import format_input, format_target\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\n    \"BorderBasisGenerator\",\n    \"BorderBasisCalculator\",\n    \"format_input\",\n    \"format_target\",\n    \"success_rate\",\n    \"run_training\",\n]\n","border_basis/core/algorithm.py":"\"\"\"\nBorder basis algorithm (Algorithm 4.1 BBasis) from\n*Computational Algebra with Attention: Transformer Oracles for Border Basis Algorithms*\n(Kera, Pelleriti, Ishihara, Zimmer, Pokutta — arXiv:2505.23696, 2025).\n\nThis file is a verbatim port of\n`HiroshiKERA/OracleBorderBasis/src/border_basis_lib/border_basis.py`.\n\nLicense: see https://github.com/HiroshiKERA/OracleBorderBasis (MIT)\n\"\"\"\n\n# ruff: noqa\n# fmt: off\nfrom sage.all import *\nfrom typing import List, Tuple\nfrom pprint import pprint\nfrom time import time\n\n# for faster computation\nfrom sortedcontainers import SortedList\n\n\nclass BorderBasisCalculator:\n    def __init__(self, ring):\n        \"\"\"Initialize with a polynomial ring.\"\"\"\n        self.ring = ring\n        self.variables = ring.gens()\n\n        # sorting time\n        self.sorting_time = 0\n\n    def terms_up_to_degree(self, d: int) -> List:\n        \"\"\"Compute all terms up to degree d.\"\"\"\n        n = len(self.variables)\n        terms = [self.ring(1)]\n        for t in range(1, d + 1):\n            exponents = list(WeightedIntegerVectors(t, [1] * n))\n            terms.extend([self.ring.monomial(*e) for e in exponents])\n        return terms\n\n    def is_all_divisors_in(self, t, O: List) -> bool:\n        \"\"\"Check if adding term t to order ideal O maintains the order ideal property.\"\"\"\n        if t.degree() in (0, 1):\n            return True\n        return all([xi in O and t / xi in O for xi in self.variables if xi.divides(t)])\n\n    def border(self, O: List) -> List:\n        \"\"\"Compute the border of an order ideal O.\"\"\"\n        border = list()\n        for t in O:\n            for var in self.variables:\n                new_term = t * var\n                if new_term not in O and new_term not in border:\n                    border.append(new_term)\n        return border\n\n    def extend_V(self, V: List) -> List:\n        V_plus = []\n        for v in V:\n            for var in self.variables:\n                new_term = v * var\n                V_plus.append(new_term)\n        return V_plus\n\n    def compute_border_basis(self, F: List, weights: dict, use_fast_elimination=False, lstabilization_only=False) -> Tuple[List, List]:\n        \"\"\"Algorithm 4.1 (BBasis).\"\"\"\n        timings = {}\n\n        s = time()\n        d = max(f.degree() for f in F)\n        timings['step1_initial_degree'] = time() - s\n\n        while True:\n            s = time()\n            L = self.terms_up_to_degree(d)\n            M = self.compute_lstable_span(F, L, use_fast_elimination=use_fast_elimination)\n            timings['step2_lstable_span'] = time() - s\n\n            s = time()\n            terms_d = [t for t in L if t.degree() == d]\n            lt_M = set(f.lm() for f in M)\n            sufficient_universe = all(t in lt_M for t in terms_d)\n            timings['step3_check_universe'] = time() - s\n\n            if not sufficient_universe:\n                d += 1\n                continue\n\n            s = time()\n            d_old = d\n            d = len(self.terms_up_to_degree(d)) - len(M)\n            if d <= d_old:\n                M = [f for f in M if f.degree() <= d]\n            else:\n                M = self.compute_lstable_span(F, self.terms_up_to_degree(d), use_fast_elimination=use_fast_elimination)\n            timings['step4_adjust_dimension'] = time() - s\n            break\n\n        if not lstabilization_only:\n            s = time()\n            O = self.find_optimal_order_ideal(M, d, weights)\n            timings['step5_find_order_ideal'] = time() - s\n\n            s = time()\n            G = self.basis_transformation(M, O, use_fast_elimination=use_fast_elimination)\n            timings['step6_compute_basis'] = time() - s\n\n            timings['total_time'] = sum(timings.values())\n        else:\n            O = []\n            G = M\n\n        return G, O, timings\n\n    def basis_transformation(self, M: List, O: List, use_fast_elimination=False) -> List:\n        \"\"\"Algorithm 4.3 (BasisTransformation).\"\"\"\n        if not M:\n            return []\n\n        max_deg = max(p.degree() for p in M)\n\n        def term_key(p):\n            lt = p.lm()\n            return (lt not in O, lt)\n\n        M_sorted = sorted(M, key=term_key)\n        # gaussian_elimination returns (W, ...) — unpack the polynomial list.\n        Gprime = self.gaussian_elimination([], M_sorted, use_fast_elimination=use_fast_elimination)[0]\n\n        border = self.border(O)\n        G = [g for g in Gprime if g.lm() in border]\n\n        return G\n\n    def gaussian_elimination_fast(self, V: List, G: List) -> List:\n        \"\"\"Optimized Gaussian elimination using SortedList + binary search.\"\"\"\n        assert (all([v.lc() == 1 for v in V]))\n\n        reducers = SortedList(V, key=lambda p: p.lm())\n        W = []\n        non_zero_reductions_indices = []\n\n        from collections import defaultdict\n        reduction_indices = defaultdict(list)\n        reducer_indices = {}\n\n        for idx, f in enumerate(G):\n            if f == 0:\n                continue\n\n            reduced = True\n            while reduced and f != 0:\n                reduced = False\n                f_lead_term = f.lm()\n\n                index = reducers.bisect_key_left(f_lead_term)\n                if index < len(reducers) and reducers[index].lm() == f_lead_term:\n                    reducer = reducers[index]\n                    f = f - f.lc() * reducer\n                    reduced = True\n                    if index >= len(V):\n                        reduction_indices[idx].append(reducer_indices[index])\n\n            if f != 0:\n                f = f / f.lc()\n                reducers.add(f)\n                W.append(f)\n                non_zero_reductions_indices.append(idx)\n                reducer_indices[len(reducers) - 1] = idx\n                if not idx in reduction_indices.keys():\n                    reduction_indices[idx] = []\n\n        return W, non_zero_reductions_indices, reduction_indices\n\n    def gaussian_elimination(self, V: List, G: List, use_fast_elimination=False) -> List:\n        \"\"\"Algorithm 2.11 (GaussEl).\"\"\"\n        assert (all([v.lc() == 1 for v in V]))\n\n        if use_fast_elimination:\n            s = time()\n            V = sorted(V, key=lambda p: p.lm())\n            self.sorting_time += time() - s\n            return self.gaussian_elimination_fast(V, G)\n\n        reducers = list(V)\n        H = list(G)\n\n        while H:\n            f = H.pop(0)\n            if f == 0:\n                continue\n\n            reduced = True\n            while reduced and f != 0:\n                reduced = False\n                for reducer in reducers:\n                    if reducer.lm() == f.lm():\n                        f = f - f.lc() * reducer\n                        reduced = True\n                        break\n\n            if f != 0:\n                f = f / f.lc()\n                reducers.append(f)\n\n        W = reducers[len(V):]\n        return W, None\n\n    def compute_lstable_span(self, F: List, L: List, use_fast_elimination=False) -> List:\n        \"\"\"Algorithm 2.13 (LStabSpan).\"\"\"\n        d = max(l.degree() for l in L)\n        # gaussian_elimination returns (W, ...) — unpack the polynomial list.\n        V = self.gaussian_elimination([], F, use_fast_elimination=use_fast_elimination)[0]\n\n        while True:\n            W = self.gaussian_elimination(V, self.border(V), use_fast_elimination=use_fast_elimination)[0]\n            W = [w for w in W if w.degree() <= d]\n\n            if W:\n                V.extend(W)\n            else:\n                break\n\n        return V\n\n    def find_optimal_order_ideal(self, M: List, d: int, weights: dict) -> List:\n        \"\"\"Lemma 3.9: alternative description of order ideal polytope (MILP).\"\"\"\n        from scipy.optimize import milp, Bounds, LinearConstraint\n        from itertools import combinations\n        import numpy as np\n\n        terms = self.terms_up_to_degree(d - 1)\n        n_terms = len(terms)\n\n        c = np.array([-weights.get(term, 0) for term in terms])\n\n        integrality = np.ones(n_terms, dtype=np.int32)\n        bounds = Bounds(0, 1)\n\n        constraints = []\n\n        # Constraint (3.6a): Order ideal property\n        order_rows = []\n        for i, t1 in enumerate(terms):\n            for j, t2 in enumerate(terms):\n                if t1.divides(t2) and t1 != t2:\n                    row = np.zeros(n_terms)\n                    row[i] = -1\n                    row[j] = 1\n                    order_rows.append(row)\n\n        if order_rows:\n            A_order = np.vstack(order_rows)\n            constraints.append(LinearConstraint(A_order, -np.inf, 0))\n\n        # Constraint (3.6b): Size of order ideal is d\n        A_eq = np.ones((1, n_terms))\n        constraints.append(LinearConstraint(A_eq, d, d))\n\n        M_d_minus_1 = [f for f in M if f.degree() <= d - 1]\n        M_size = len(M_d_minus_1)\n\n        coeff_matrix = np.zeros((len(M_d_minus_1), len(terms)))\n        for i, f in enumerate(M_d_minus_1):\n            for j, term in enumerate(terms):\n                coeff = f.monomial_coefficient(term)\n                if coeff != 0:\n                    coeff_matrix[i, j] = float(coeff)\n\n        def get_submatrix_rank(U_indices):\n            return np.linalg.matrix_rank(coeff_matrix[:, U_indices])\n\n        # Constraint (3.6c)\n        rank_rows = []\n        rank_lbs = []\n\n        for U_indices in combinations(range(n_terms), M_size):\n            rank_U = get_submatrix_rank(U_indices)\n            row = np.zeros(n_terms)\n            row[list(U_indices)] = 1\n            rank_rows.append(row)\n            rank_lbs.append(M_size - rank_U)\n\n        if rank_rows:\n            A_rank = np.vstack(rank_rows)\n            constraints.append(LinearConstraint(A_rank, rank_lbs, np.inf))\n\n        options = {\n            'disp': False,\n            'presolve': True,\n            'mip_rel_gap': 1e-4,\n        }\n\n        result = milp(\n            c=c,\n            integrality=integrality,\n            bounds=bounds,\n            constraints=constraints,\n            options=options,\n        )\n\n        if not result.success:\n            raise ValueError(f\"MILP failed: {result.message}\")\n\n        O = list(set(term for i, term in enumerate(terms) if result.x[i] > 0.5))\n        O = sorted(O, key=lambda t: t)\n        return O\n","border_basis/core/formatter.py":"\"\"\"\nConvert SageMath polynomial objects to tokenizable strings for the border basis task.\n\nFormat\n------\nInput  : \"g1 | g2 | g3\"        (generators separated by ' | ')\nTarget : \"b1 | b2 | ... | bk\"  (border basis elements)\n\nExample\n-------\ngenerators:   g1 = x^2 + 6,  g2 = y^2 + x,  g3 = x*y + 1  (over GF(7))\nborder basis: B = [x + 1, y + 2, ...]\n→ input  = \"x^2 + 6 | y^2 + x | x*y + 1\"\n→ target = \"x + 1 | y + 2 | ...\"\n\"\"\"\n\nSEP = \" | \"\n\n\ndef poly_to_str(p) -> str:\n    \"\"\"Return the default SageMath string representation of a polynomial.\"\"\"\n    return str(p)\n\n\ndef format_input(generators: list) -> str:\n    \"\"\"\n    Concatenate all generators into a single input string.\n\n    Example: [g1, g2, g3] → \"g1 | g2 | g3\"\n    \"\"\"\n    return SEP.join(poly_to_str(g) for g in generators)\n\n\ndef format_target(basis: list) -> str:\n    \"\"\"\n    Concatenate all border basis elements into a single target string.\n\n    Example: [b1, b2, b3] → \"b1 | b2 | b3\"\n    \"\"\"\n    return SEP.join(poly_to_str(b) for b in basis)\n","border_basis/core/generator.py":"\"\"\"\nGenerate (problem, answer) pairs for the border basis task.\n\nMath\n----\nR = k[x, y]   (typically k = GF(p) for guaranteed 0-dimensional ideals)\nI = ⟨g1, g2, g3⟩ ⊂ R\n\nGiven F = [g1, g2, g3] sampled at random, the learning task is to predict\nthe **border basis** G of I (Algorithm 4.1 BBasis from the reference paper).\n\nReference\n---------\nImplements the data-generation pattern for the paper\n*Computational Algebra with Attention: Transformer Oracles for Border Basis\nAlgorithms* (Kera, Pelleriti, Ishihara, Zimmer, Pokutta — arXiv:2505.23696, 2025).\n\nThe underlying algorithm lives in `border_basis.core.algorithm.BorderBasisCalculator`,\nwhich is a verbatim port of\n`HiroshiKERA/OracleBorderBasis/src/border_basis_lib/border_basis.py`.\n\nClass-based generator pattern matches `issac2026_experiments/groebner/generate_dataset.py`.\n\"\"\"\n\nimport sage.all  # noqa: F401  # ensure Sage is initialised\nimport sage.misc.randstate as randstate  # type: ignore\n\nfrom calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\nfrom .algorithm import BorderBasisCalculator\n\n\nclass BorderBasisGenerator:\n    \"\"\"\n    Problem generator: F = [g1, ..., g_{num_polynomials}],\n    answer G = border basis of ⟨F⟩.\n\n    Parameters\n    ----------\n    sampler : PolynomialSampler\n        Builds the random polynomials. Best used with a GF(p) field so that\n        the ideal is zero-dimensional with high probability.\n    num_polynomials : int\n        Number of generators (default 3).\n    use_fast_elimination : bool\n        If True, the Gaussian elimination step uses a sorted-list optimisation.\n    lstabilization_only : bool\n        If True, stop at the L-stable span (no order-ideal optimisation +\n        no basis transformation). Much faster, but you only get an intermediate\n        basis rather than the final border basis.\n    \"\"\"\n\n    def __init__(\n        self,\n        sampler: PolynomialSampler,\n        num_polynomials: int = 3,\n        use_fast_elimination: bool = True,\n        lstabilization_only: bool = False,\n    ):\n        self.sampler = sampler\n        self.num_polynomials = num_polynomials\n        self.use_fast_elimination = use_fast_elimination\n        self.lstabilization_only = lstabilization_only\n\n    def __call__(self, seed: int) -> tuple[list, list]:\n        randstate.set_random_seed(seed)\n        R = self.sampler.get_ring()\n        calculator = BorderBasisCalculator(R)\n\n        max_retries = 100\n        last_F, last_G = None, None\n        for _ in range(max_retries):\n            try:\n                F = self.sampler.sample(num_samples=self.num_polynomials)\n\n                # Border basis requires a 0-dimensional ideal.\n                ideal = R.ideal(F)\n                if ideal.dimension() != 0:\n                    continue\n\n                # Uniform weights over all terms up to max input degree.\n                d = max(f.degree() for f in F)\n                terms = calculator.terms_up_to_degree(d)\n                weights = {t: 1 for t in terms}\n\n                G, _O, _timings = calculator.compute_border_basis(\n                    F,\n                    weights,\n                    use_fast_elimination=self.use_fast_elimination,\n                    lstabilization_only=self.lstabilization_only,\n                )\n\n                last_F, last_G = list(F), list(G)\n                if last_G:\n                    return last_F, last_G\n            except Exception:\n                # MILP / Sage edge case — try next seed.\n                continue\n\n        return last_F, last_G\n","border_basis/core/metrics.py":"\"\"\"\nMetrics for the border basis task.\n\nsuccess_rate() is called after training to report evaluation accuracy.\nExact string match: the model wins only when its output is character-for-character\nidentical to the ground-truth border basis string.\n\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"\n    Return the fraction of predictions that exactly match the target.\n\n    The ordering of border basis elements is deterministic (fixed by SageMath),\n    so exact string match is the correct metric.\n    \"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","border_basis/core/parser.py":"\"\"\"\nLoad-time preprocessor for the border basis task.\n\nFor the default pipeline (text-based storage), no custom preprocessor is needed:\nCALT's text pipeline reads \"g1 | g2 | g3 # b1 | ... | bk\" directly.\n\nThis module is reserved for future extensions (e.g., alternative monomial orders).\n\"\"\"\n","border_basis/core/train.py":"\"\"\"\nTraining runner for the border basis task.\n\nOrchestrates the four CALT pipelines:\n  1. IOPipeline      — load text data, tokenize with UnifiedLexer\n  2. ModelPipeline   — build the Transformer\n  3. TrainerPipeline — train (HuggingFace Trainer under the hood)\n  4. evaluate_and_save_generation — decode predictions, compute success rate\n\nLexer format\n------------\nIf `data.lexer_config` points to a file with C/E expanded vocab, the pipeline\nautomatically wires `ExpandedFormLoadPreprocessor` to transform raw polynomial\nstrings into \"C1 E2 E0 + ...\" format before tokenization.\n\nUsage\n-----\n    from border_basis.core.train import run_training\n    from shared.config import load_config\n\n    cfg = load_config(\"configs/train.yaml\")\n    success_rate = run_training(cfg, data_cfg=load_config(\"configs/data.yaml\"))\n\"\"\"\n\nimport os\n\nimport sage.all  # noqa: F401  # initialise Sage before any submodule\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom shared.calt_adapter import (\n    ChainLoadPreprocessor,\n    ExpandedFormLoadPreprocessor,\n    ModelPipeline,\n    TextToSageLoadPreprocessor,\n    TrainerPipeline,\n    apply_dryrun_settings,\n    build_io_pipeline_with_cache,\n    build_ring_from_sampler,\n    detect_lexer_format,\n)\n\n\ndef build_load_preprocessor(cfg: DictConfig, data_cfg: DictConfig | None):\n    \"\"\"\n    Build the load-time preprocessor chain. Used by run_training AND the offline\n    preprocess.py so the two stay in sync.\n\n    For border_basis the only load preprocessor is ExpandedFormLoadPreprocessor\n    (when lexer_format == \"expanded\"). Otherwise None.\n\n    Returns\n    -------\n    tuple (preprocessor_or_None, format_str)\n    \"\"\"\n    lexer_format = detect_lexer_format(cfg.data.lexer_config)\n\n    chain = []\n    if lexer_format == \"expanded\":\n        if data_cfg is None:\n            raise ValueError(\"lexer format='expanded' requires data_cfg to rebuild the source ring.\")\n        R_src = build_ring_from_sampler(data_cfg.sampler)\n        chain.append(TextToSageLoadPreprocessor(delimiter=\"|\", ring=R_src))\n        chain.append(ExpandedFormLoadPreprocessor(delimiter=\" | \"))\n\n    # Wire optional user post-processing hooks (task 3). See\n    # `shared/user_postprocessor.py` for details. Both slots are opt-in:\n    # if neither postprocessor file exists, this branch is a no-op.\n    from shared.user_postprocessor import (\n        get_user_postprocessors,\n        RawLineToTupleAdapter,\n        UserPostProcessorAdapter,\n    )\n    user_hooks = get_user_postprocessors(\"border_basis\")\n    if user_hooks:\n        if not chain:\n            chain.append(RawLineToTupleAdapter())\n        chain.append(UserPostProcessorAdapter(user_hooks))\n\n    if not chain:\n        return None, lexer_format\n    if len(chain) == 1:\n        return chain[0], lexer_format\n    return ChainLoadPreprocessor(*chain), lexer_format\n\n\ndef run_training(cfg: DictConfig, data_cfg: DictConfig | None = None, dryrun: bool = False) -> float:\n    \"\"\"\n    Run the full training pipeline for the border basis task.\n\n    Parameters\n    ----------\n    cfg : DictConfig\n        Loaded train.yaml.\n    data_cfg : DictConfig | None\n        Loaded data.yaml. Required when the lexer is in expanded format\n        (we need to rebuild the source ring to call TextToSageLoadPreprocessor).\n    dryrun : bool\n\n    Returns\n    -------\n    float : exact-match success rate on the test set.\n    \"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    # Cache hierarchy (pretok → strings → raw) is handled by the CALT library.\n    # `training_order` is hard-coded \"border\" for border_basis (there is no FGLM\n    # variant) so we use that as the hash discriminator.\n    from shared.user_postprocessor import user_postprocessor_hash_contribution\n\n    hooks_hash = user_postprocessor_hash_contribution(\"border_basis\")\n\n    load_preprocessor, lexer_format = build_load_preprocessor(cfg, data_cfg)\n    training_order = \"border\"  # constant for this task\n    io_pipeline, cache_kind = build_io_pipeline_with_cache(\n        cfg, data_cfg, training_order, lexer_format, load_preprocessor,\n        extra_hash_bytes=hooks_hash,\n    )\n    print(f\"[run_training] cache={cache_kind} (format={lexer_format})\")\n\n    io_dict = io_pipeline.build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","border_basis/experiments/ablation/README.md":"# Border basis — ablation experiment\n\n**Goal**: vary the model size and observe how performance changes on the toy\nborder-basis dataset (GF(7), 2 variables).\n\n| Knob | Choices |\n|------|---------|\n| `--model_size` | `small`, `medium`, `large` |\n\n## How to run\n```bash\ncd border_basis/experiments/ablation/scripts\npython train.py --model_size large\n```\n\nReuses the dataset generated by the toy experiment (`../../toy/data/GF7/`).\nOutputs land in `outputs/results_<model_size>/`.\n","border_basis/experiments/ablation/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 3\n  num_encoder_heads: 4\n  num_decoder_layers: 3\n  num_decoder_heads: 4\n  d_model: 256\n  encoder_ffn_dim: 1024\n  decoder_ffn_dim: 1024\n  max_sequence_length: 512\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0003\n  weight_decay: 0.0\n  warmup_ratio: 0.05\n  batch_size: 32\n  test_batch_size: 32\n  lr_scheduler_type: linear\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 4\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: border_basis_ablation\n    name: ablation\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../../toy/data/GF7/train_raw.txt\n  test_dataset_path: ../../toy/data/GF7/test_raw.txt\n  lexer_config: ../../toy/configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","border_basis/experiments/ablation/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","border_basis/experiments/ablation/scripts/train.py":"\"\"\"Ablation: vary model size for the border basis task.\n\nUsage: python train.py --model_size {small,medium,large}\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom border_basis.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\nMODEL_SIZES = {\n    \"small\":  dict(d_model=128, num_encoder_layers=2, num_decoder_layers=2,\n                   encoder_ffn_dim=512, decoder_ffn_dim=512,\n                   num_encoder_heads=4, num_decoder_heads=4),\n    \"medium\": dict(d_model=256, num_encoder_layers=4, num_decoder_layers=4,\n                   encoder_ffn_dim=1024, decoder_ffn_dim=1024,\n                   num_encoder_heads=8, num_decoder_heads=8),\n    \"large\":  dict(d_model=512, num_encoder_layers=6, num_decoder_layers=6,\n                   encoder_ffn_dim=2048, decoder_ffn_dim=2048,\n                   num_encoder_heads=8, num_decoder_heads=8),\n}\n\n\n@click.command()\n@click.option(\"--model_size\", type=click.Choice(list(MODEL_SIZES)), default=\"medium\")\n@click.option(\"--dryrun\", is_flag=True)\ndef main(model_size: str, dryrun: bool) -> None:\n    cfg = OmegaConf.load(config_dir(__file__) / \"train.yaml\")\n    cfg.model = OmegaConf.create({**OmegaConf.to_container(cfg.model), **MODEL_SIZES[model_size]})\n    cfg.train.save_dir = f\"../outputs/results_{model_size}\"\n    cfg.train.wandb.name = f\"ablation_{model_size}\"\n\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"[{model_size}] Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n","border_basis/experiments/finite_field/README.md":"# Border basis — finite_field experiment\n\n**Goal**: vary the size of the base finite field GF(p) and observe how the border\nbasis structure changes with p.\n\n`p ∈ {2, 5, 7, 11}` is selectable via CLI flag.\n\n## How to run\n```bash\ncd border_basis/experiments/finite_field/scripts\npython generate.py --p 5         # → ../data/GF5/\npython train.py --p 5\n```\n\nEach `p` writes its dataset to `../data/GF{p}/` and the trained model to\n`../outputs/results_GF{p}/`, so runs do not clash.\n","border_basis/experiments/finite_field/configs/data.yaml":"# Border basis finite_field: generate.py overrides field_str via --p flag.\n\nsampler:\n  symbols: \"x,y\"\n  field_str: \"GF7\"        # overridden by generate.py --p\n  order: \"degrevlex\"\n  max_num_terms: 4\n  max_degree: 3\n  min_degree: 1\n  max_coeff: 0\n  degree_sampling: \"uniform\"\n  term_sampling: \"uniform\"\n\nproblem_generator:\n  num_polynomials: 3\n\ndataset:\n  save_dir: \"../data/GF7\" # overridden by generate.py --p\n  num_train_samples: 5000\n  num_test_samples: 500\n  batch_size: 500\n  n_jobs: 8\n  root_seed: 42\n  verbose: true\n  backend: \"sagemath\"\n  save_text: true\n  save_json: false\n","border_basis/experiments/finite_field/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 3\n  num_encoder_heads: 4\n  num_decoder_layers: 3\n  num_decoder_heads: 4\n  d_model: 256\n  encoder_ffn_dim: 1024\n  decoder_ffn_dim: 1024\n  max_sequence_length: 512\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 50\n  learning_rate: 0.0003\n  weight_decay: 0.0\n  warmup_ratio: 0.05\n  batch_size: 16\n  test_batch_size: 16\n  lr_scheduler_type: linear\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: border_basis\n    name: finite_field_GF7\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/GF7/train_raw.txt\n  test_dataset_path: ../data/GF7/test_raw.txt\n  lexer_config: ../../toy/configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","border_basis/experiments/finite_field/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","border_basis/experiments/finite_field/scripts/generate.py":"\"\"\"Generate border basis data over GF(p), p selectable via --p.\n\nUsage: python generate.py --p 5\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\nfrom calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\nfrom border_basis.core.generator import BorderBasisGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--p\", type=int, default=None, help=\"Prime for GF(p).\")\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\ndef main(p: int | None, config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n\n    if p is not None:\n        cfg.sampler.field_str = f\"GF{p}\"\n        cfg.dataset.save_dir = f\"../data/GF{p}\"\n\n    set_seed(cfg.dataset.root_seed)\n\n    sampler_cfg = dict(OmegaConf.to_container(cfg.sampler, resolve=True))\n    sampler = PolynomialSampler(**sampler_cfg)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True)\n    problem_generator = BorderBasisGenerator(sampler=sampler, **gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(\n        cfg.dataset,\n        instance_generator=problem_generator,\n    )\n    pipeline.run()\n    print(f\"Dataset generation completed (GF{p or cfg.sampler.field_str[2:]})\")\n\n\nif __name__ == \"__main__\":\n    main()\n","border_basis/experiments/finite_field/scripts/train.py":"\"\"\"Train border basis model over GF(p).\n\nUsage: python train.py --p 5\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom border_basis.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--p\", type=int, default=None, help=\"Prime for GF(p).\")\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\n@click.option(\"--dryrun\", is_flag=True)\ndef main(p: int | None, config_path: str | None, dryrun: bool) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n\n    if p is not None:\n        cfg.data.train_dataset_path = f\"../data/GF{p}/train_raw.txt\"\n        cfg.data.test_dataset_path = f\"../data/GF{p}/test_raw.txt\"\n        cfg.train.save_dir = f\"../outputs/results_GF{p}\"\n        if hasattr(cfg.train, \"wandb\") and hasattr(cfg.train.wandb, \"name\"):\n            cfg.train.wandb.name = f\"finite_field_GF{p}\"\n\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n","border_basis/experiments/toy/configs/data.yaml":"# Border basis task (toy): G=[g1,g2,g3] over GF(7)[x,y] -> border basis of (G)\n\nsampler:\n  symbols: \"x,y\"\n  field_str: \"GF7\"\n  order: \"degrevlex\"\n  max_num_terms: 4\n  max_degree: 3\n  min_degree: 1\n  max_coeff: 0\n  degree_sampling: \"uniform\"\n  term_sampling: \"uniform\"\n\nproblem_generator:\n  num_polynomials: 3\n\ndataset:\n  save_dir: \"../data/GF7\"\n  num_train_samples: 1000\n  num_test_samples: 200\n  batch_size: 100\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sagemath\"\n  save_text: true\n  save_json: false\n","border_basis/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", -10, 10]  # GF(7) coefficients are 0-6\n  misc: [\"+\", \"-\", \"*\", \"^\", \"x\", \"y\", \"|\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 1\n  allow_float: false\n\nstrict: true\ninclude_base_vocab: true\n","border_basis/experiments/toy/configs/lexer_expanded.yaml":"# Lexer EXPANDED — C/E form for the border basis task (GF(p)[x, y]).\n#\n# Same C/E scheme as groebner, but the coefficient range is narrower\n# (modular arithmetic in GF(7) gives coefficients in 0..6).\n#\n# Example: \"2*x*y + 5*y + 1\" → \"C2 E1 E1 + C5 E0 E1 + C1 E0 E0\"\n#\n# Reference: ISSAC '26 paper §2.2.\n\nvocab:\n  range:\n    coefficients: [\"C\", 0, 6]     # GF(7): coefficients are 0..6\n    exponents:    [\"E\", 0, 5]     # max_degree=3 + safety margin\n  misc: [\"+\", \"|\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 0\n  allow_float: false\n\nstrict: true\ninclude_base_vocab: true\n","border_basis/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 3\n  num_encoder_heads: 4\n  num_decoder_layers: 3\n  num_decoder_heads: 4\n  d_model: 256\n  encoder_ffn_dim: 1024\n  decoder_ffn_dim: 1024\n  max_sequence_length: 512\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 50\n  learning_rate: 0.0003\n  weight_decay: 0.0\n  warmup_ratio: 0.05\n  batch_size: 16\n  test_batch_size: 16\n  lr_scheduler_type: linear\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: border_basis\n    name: toy_gf7\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/GF7/train_raw.txt\n  test_dataset_path: ../data/GF7/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","border_basis/experiments/toy/scripts/evaluate.py":"\"\"\"Print evaluation results for the border basis toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    show_examples(generated, references, n=5, successes=False)\n","border_basis/experiments/toy/scripts/generate.py":"\"\"\"Generate the border basis dataset for the toy experiment.\n\nReference: issac2026_experiments/groebner/generate_dataset.py.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\nfrom calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\nfrom border_basis.core.generator import BorderBasisGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML (defaults to ../configs/data.yaml).\",\n)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    sampler_cfg = dict(OmegaConf.to_container(cfg.sampler, resolve=True))\n    sampler = PolynomialSampler(**sampler_cfg)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True)\n    problem_generator = BorderBasisGenerator(sampler=sampler, **gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(\n        cfg.dataset,\n        instance_generator=problem_generator,\n    )\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","border_basis/experiments/toy/scripts/preprocess.py":"\"\"\"Materialize a preprocessing cache for the toy border basis experiment.\n\nMirrors groebner_basis/experiments/toy/scripts/preprocess.py — see that file\nfor the rationale. By default builds a *pre-tokenized* cache so that training\nperforms zero tokenization at runtime.\n\nUsage\n-----\n    cd border_basis/experiments/toy/scripts\n    python preprocess.py                     # pretokenized cache (default)\n    python preprocess.py --no-pretokenize    # strings cache (skip SageMath only)\n    python preprocess.py --force             # rebuild even if hash matches\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom border_basis.core.train import build_load_preprocessor\nfrom shared.calt_adapter import preprocess_to_ids, run_preprocess\nfrom shared.paths import config_dir\nfrom shared.user_postprocessor import user_postprocessor_hash_contribution\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to train config YAML (defaults to ../configs/train.yaml).\",\n)\n@click.option(\n    \"--data_config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML (defaults to ../configs/data.yaml).\",\n)\n@click.option(\"--force\", is_flag=True, help=\"Rebuild even if hash matches.\")\n@click.option(\n    \"--pretokenize/--no-pretokenize\",\n    default=True,\n    help=\"Pre-tokenize all the way to input_ids (default ON).\",\n)\ndef main(\n    config_path: str | None,\n    data_config_path: str | None,\n    force: bool,\n    pretokenize: bool,\n) -> None:\n    \"\"\"Preprocess raw .txt → JSONL cache for fast training startup.\"\"\"\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n\n    data_path = Path(data_config_path) if data_config_path else config_dir(__file__) / \"data.yaml\"\n    data_cfg = OmegaConf.load(data_path) if data_path.exists() else None\n\n    load_preprocessor, lexer_format = build_load_preprocessor(cfg, data_cfg)\n    training_order = \"border\"  # constant for this task\n    hooks_hash = user_postprocessor_hash_contribution(\"border_basis\")\n\n    if pretokenize:\n        cache_dir = preprocess_to_ids(\n            cfg, data_cfg, training_order, lexer_format, load_preprocessor,\n            force=force, extra_hash_bytes=hooks_hash,\n        )\n    else:\n        cache_dir = run_preprocess(\n            cfg, data_cfg, training_order, lexer_format, load_preprocessor,\n            force=force, extra_hash_bytes=hooks_hash,\n        )\n    print(f\"[done] cache at: {cache_dir}\")\n\n\nif __name__ == \"__main__\":\n    main()\n","border_basis/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full border_basis/toy experiment end-to-end.\n#\n# Usage:\n#   bash run.sh                          # defaults: raw lexer, with pretok cache\n#   bash run.sh --lexer expanded         # use C/E expanded vocab\n#   bash run.sh --no-preprocess          # skip preprocess.py\n#   bash run.sh --dryrun                 # fast smoke test\n#   bash run.sh --force                  # rebuild caches even if hash matches\n\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\n\nPY=\"${PYTHON:-python}\"\nif ! \"$PY\" -c \"import calt, sage\" >/dev/null 2>&1; then\n    echo \"❌ Wrong Python environment: \\`$PY\\` cannot import \\`calt\\` or \\`sage\\`.\" >&2\n    echo \"   Run:  conda activate calt-env   (or set PYTHON=/path/to/calt-env/bin/python)\" >&2\n    exit 1\nfi\n\nLEXER=\"raw\"\nPREPROCESS=1\nDRYRUN=\"\"\nFORCE=\"\"\nwhile [[ $# -gt 0 ]]; do\n    case $1 in\n        --lexer)            LEXER=\"$2\"; shift 2 ;;\n        --no-preprocess)    PREPROCESS=0; shift ;;\n        --dryrun)           DRYRUN=\"--dryrun\"; shift ;;\n        --force)            FORCE=\"--force\"; shift ;;\n        -h|--help) sed -n '2,9p' \"$0\" | sed 's/^# \\?//'; exit 0 ;;\n        *) echo \"Unknown flag: $1\" >&2; exit 2 ;;\n    esac\ndone\n\ncase $LEXER in\n    raw|expanded) ;;\n    *) echo \"Invalid --lexer $LEXER (must be raw or expanded)\" >&2; exit 2 ;;\nesac\n\nLEXER_PATH=\"../configs/lexer.yaml\"\n[[ \"$LEXER\" == \"expanded\" ]] && LEXER_PATH=\"../configs/lexer_expanded.yaml\"\n\necho \"=================================================================\"\necho \"  border_basis/toy — end-to-end run\"\necho \"  lexer   : $LEXER  ($LEXER_PATH)\"\necho \"  preproc : $([ $PREPROCESS -eq 1 ] && echo enabled || echo skipped)\"\necho \"  dryrun  : $([ -n \"$DRYRUN\" ] && echo YES || echo no)\"\necho \"=================================================================\"\n\nTMP_TRAIN_YAML=\"$(mktemp --suffix=.yaml)\"\ntrap 'rm -f \"$TMP_TRAIN_YAML\"' EXIT\nsed \"s|lexer_config:.*|lexer_config: $LEXER_PATH|\" ../configs/train.yaml > \"$TMP_TRAIN_YAML\"\n\nif [[ ! -f ../data/GF7/train_raw.txt ]]; then\n    echo \"=== Step 1/3: Generate data ===\"\n    \"$PY\" generate.py\nelse\n    echo \"=== Step 1/3: Generate data (already exists, skipping) ===\"\nfi\n\nif [[ $PREPROCESS -eq 1 ]]; then\n    echo \"=== Step 2/3: Preprocess (build cache) ===\"\n    \"$PY\" preprocess.py --config_path \"$TMP_TRAIN_YAML\" $FORCE\nelse\n    echo \"=== Step 2/3: Preprocess SKIPPED ===\"\nfi\n\necho \"=== Step 3/3: Train + Evaluate ===\"\n\"$PY\" train.py --config_path \"$TMP_TRAIN_YAML\" $DRYRUN\n\necho \"=================================================================\"\necho \"  DONE. Output: ../outputs/results/\"\necho \"=================================================================\"\n","border_basis/experiments/toy/scripts/train.py":"\"\"\"Train the border basis model for the toy experiment.\n\nIf the lexer.yaml referenced in train.yaml is in C/E expanded format, the\ndata_config (--data_config_path) is required to rebuild the source ring.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom border_basis.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to train config YAML (defaults to ../configs/train.yaml).\",\n)\n@click.option(\n    \"--data_config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML. Required if lexer is in expanded format.\",\n)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(config_path: str | None, data_config_path: str | None, dryrun: bool) -> None:\n    \"\"\"Train a Transformer to compute border bases from polynomial generators.\"\"\"\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n\n    data_cfg = None\n    data_path = Path(data_config_path) if data_config_path else config_dir(__file__) / \"data.yaml\"\n    if data_path.exists():\n        data_cfg = OmegaConf.load(data_path)\n\n    success = run_training(cfg, data_cfg=data_cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"},"eigvec_3x3":{"eigvec_3x3/README.md":"# eigvec_3x3\n\n**Task**: Dominant eigenvector of a 3x3 symmetric matrix (real-valued, finite precision).\n\n*Adapted from the official [calt-codebase](https://github.com/HiroshiKERA/calt-codebase) examples.*\n\n## Data format\n\n```\nInput  : 2.10,0.30,-0.50;0.30,1.40,0.20;-0.50,0.20,3.10\nOutput : -0.20,0.10,0.97\n```\n\n## Quick start\n\n```bash\ncd eigvec_3x3/experiments/toy/scripts\npython generate.py    # 1. build the dataset  -> ../data/\npython train.py       # 2. train the model    -> ../outputs/results/\npython evaluate.py    # 3. report the success rate\n```\n\nOr `bash run.sh` (add `--dryrun` for a quick smoke test).\n","eigvec_3x3/core/__init__.py":"from .generator import Eigvec3x3Generator\nfrom .formatter import format_input, format_target\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\"Eigvec3x3Generator\", \"format_input\", \"format_target\", \"success_rate\", \"run_training\"]\n","eigvec_3x3/core/formatter.py":"\"\"\"String format for the eigvec_3x3 task.\n\nInput  : 2.10,0.30,-0.50;0.30,1.40,0.20;-0.50,0.20,3.10\nOutput : -0.20,0.10,0.97\n\nThe generator already returns ready-to-write strings; these helpers mirror that.\n\"\"\"\n\n\ndef format_input(problem) -> str:\n    return str(problem)\n\n\ndef format_target(answer) -> str:\n    return str(answer)\n","eigvec_3x3/core/generator.py":"\"\"\"Generate (problem, answer) pairs for the 3x3 dominant-eigenvector task.\n\nAdapted from HiroshiKERA/calt-codebase examples/eigvec_3x3.\nInput  : a 3x3 symmetric PSD matrix (rows ';'-separated, entries ','-separated)\nOutput : the unit eigenvector of its largest eigenvalue, rounded.\nReal-valued data with finite precision.\n\"\"\"\n\nimport numpy as np\n\n\nclass Eigvec3x3Generator:\n    def __init__(self, decimals: int = 2):\n        self.decimals = decimals\n\n    def __call__(self, seed: int) -> tuple[str, str]:\n        rng = np.random.default_rng(seed)\n        A = rng.normal(size=(3, 3))\n        M = A.T @ A  # symmetric PSD -> real eigenvalues\n        vals, vecs = np.linalg.eigh(M)\n        v = vecs[:, int(np.argmax(vals))]\n        norm = np.linalg.norm(v)\n        if norm > 0:\n            v = v / norm\n        d = self.decimals\n        m_str = \";\".join(\",\".join(f\"{x:.{d}f}\" for x in row) for row in M.tolist())\n        v_str = \",\".join(f\"{x:.{d}f}\" for x in v.tolist())\n        return m_str, v_str\n","eigvec_3x3/core/metrics.py":"\"\"\"Metrics for the eigvec_3x3 task.\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"Fraction of predictions that exactly match the target (after .strip()).\"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","eigvec_3x3/core/parser.py":"\"\"\"Load-time preprocessor for the eigvec_3x3 task.\n\nPlain text (\"input # output\" per line) is read directly by CALT's default text\nloader — no custom preprocessor is needed.\n\"\"\"\n","eigvec_3x3/core/train.py":"\"\"\"Training runner for the eigvec_3x3 task.\n\nPlain-text storage means CALT's default text loader handles parsing, so no\ncustom load_preprocessor is needed (same simple pipeline as the parity task).\n\"\"\"\n\nimport os\n\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom shared.calt_adapter import (\n    IOPipeline,\n    ModelPipeline,\n    TrainerPipeline,\n    apply_dryrun_settings,\n)\n\n\ndef run_training(cfg: DictConfig, dryrun: bool = False) -> float:\n    \"\"\"Run the full pipeline (load -> build -> train -> evaluate).\"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    io_dict = IOPipeline.from_config(cfg.data).build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","eigvec_3x3/experiments/toy/configs/data.yaml":"# eigvec_3x3 task (toy) — adapted from HiroshiKERA/calt-codebase examples/eigvec_3x3\n\nproblem_generator:\n  decimals: 2\n\ndataset:\n  save_dir: \"../data\"\n  num_train_samples: 5000\n  num_test_samples: 500\n  batch_size: 1000\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sympy\"\n  save_text: true\n  save_json: false\n","eigvec_3x3/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", -999, 999]\n  misc: [\",\", \";\", \"[\", \"]\", \"-\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 2\n  allow_float: true\n\nstrict: false\ninclude_base_vocab: true\n","eigvec_3x3/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 160\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: eigvec_3x3\n    name: toy\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","eigvec_3x3/experiments/toy/scripts/evaluate.py":"\"\"\"Print evaluation results for the eigvec_3x3 toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n    correct = sum(g == r for g, r in zip(generated, references))\n    rate = correct / len(references) if references else 0.0\n    print(f\"Success rate: {100 * rate:.1f}%  ({correct}/{len(references)})\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","eigvec_3x3/experiments/toy/scripts/generate.py":"\"\"\"Generate the eigvec_3x3 dataset for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\n\nfrom eigvec_3x3.core.generator import Eigvec3x3Generator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True) or {}\n    problem_generator = Eigvec3x3Generator(**gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(cfg.dataset, instance_generator=problem_generator)\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","eigvec_3x3/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full eigvec_3x3/toy experiment end-to-end.\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\necho \"=== Step 1/3: Generate data ===\"\npython generate.py\necho \"=== Step 2/3: Train ===\"\npython train.py \"$@\"\necho \"=== Step 3/3: Evaluate ===\"\npython evaluate.py\n","eigvec_3x3/experiments/toy/scripts/train.py":"\"\"\"Train the eigvec_3x3 model for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom eigvec_3x3.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(config_path: str | None, dryrun: bool) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"},"gf17_addition":{"gf17_addition/README.md":"# gf17_addition\n\n**Task**: Running sums of a list of numbers, modulo 17 (finite-field arithmetic).\n\n*Adapted from the official [calt-codebase](https://github.com/HiroshiKERA/calt-codebase) examples.*\n\n## Data format\n\n```\nInput  : 5,9,3,4\nOutput : 5,14,0,4\n```\n\n## Quick start\n\n```bash\ncd gf17_addition/experiments/toy/scripts\npython generate.py    # 1. build the dataset  -> ../data/\npython train.py       # 2. train the model    -> ../outputs/results/\npython evaluate.py    # 3. report the success rate\n```\n\nOr `bash run.sh` (add `--dryrun` for a quick smoke test).\n","gf17_addition/core/__init__.py":"from .generator import Gf17AdditionGenerator\nfrom .formatter import format_input, format_target\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\"Gf17AdditionGenerator\", \"format_input\", \"format_target\", \"success_rate\", \"run_training\"]\n","gf17_addition/core/formatter.py":"\"\"\"String format for the gf17_addition task.\n\nInput  : 5,9,3,4\nOutput : 5,14,0,4\n\nThe generator already returns ready-to-write strings; these helpers mirror that.\n\"\"\"\n\n\ndef format_input(problem) -> str:\n    return str(problem)\n\n\ndef format_target(answer) -> str:\n    return str(answer)\n","gf17_addition/core/generator.py":"\"\"\"Generate (problem, answer) pairs for the GF(17) running-sum task.\n\nAdapted from HiroshiKERA/calt-codebase examples/gf17_addition.\nInput  : a list of integers in GF(17), comma-separated\nOutput : their running (cumulative) sums mod 17  (e.g. \"5,9,3,4\" -> \"5,14,0,4\")\n\"\"\"\n\nimport random\n\n\nclass Gf17AdditionGenerator:\n    def __init__(self, p: int = 17, min_len: int = 3, max_len: int = 6):\n        self.p = p\n        self.min_len = min_len\n        self.max_len = max_len\n\n    def __call__(self, seed: int) -> tuple[str, str]:\n        random.seed(seed)\n        length = random.randint(self.min_len, self.max_len)\n        numbers = [random.randint(0, self.p - 1) for _ in range(length)]\n        cumulative, s = [], 0\n        for n in numbers:\n            s = (s + n) % self.p\n            cumulative.append(s)\n        return \",\".join(map(str, numbers)), \",\".join(map(str, cumulative))\n","gf17_addition/core/metrics.py":"\"\"\"Metrics for the gf17_addition task.\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"Fraction of predictions that exactly match the target (after .strip()).\"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","gf17_addition/core/parser.py":"\"\"\"Load-time preprocessor for the gf17_addition task.\n\nPlain text (\"input # output\" per line) is read directly by CALT's default text\nloader — no custom preprocessor is needed.\n\"\"\"\n","gf17_addition/core/train.py":"\"\"\"Training runner for the gf17_addition task.\n\nPlain-text storage means CALT's default text loader handles parsing, so no\ncustom load_preprocessor is needed (same simple pipeline as the parity task).\n\"\"\"\n\nimport os\n\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom shared.calt_adapter import (\n    IOPipeline,\n    ModelPipeline,\n    TrainerPipeline,\n    apply_dryrun_settings,\n)\n\n\ndef run_training(cfg: DictConfig, dryrun: bool = False) -> float:\n    \"\"\"Run the full pipeline (load -> build -> train -> evaluate).\"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    io_dict = IOPipeline.from_config(cfg.data).build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","gf17_addition/experiments/toy/configs/data.yaml":"# gf17_addition task (toy) — adapted from HiroshiKERA/calt-codebase examples/gf17_addition\n\nproblem_generator:\n  p: 17\n  min_len: 3\n  max_len: 6\n\ndataset:\n  save_dir: \"../data\"\n  num_train_samples: 5000\n  num_test_samples: 500\n  batch_size: 1000\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sympy\"\n  save_text: true\n  save_json: false\n","gf17_addition/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", 0, 16]\n  misc: [\",\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 0\n  allow_float: false\n\nstrict: false\ninclude_base_vocab: true\n","gf17_addition/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 64\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: gf17_addition\n    name: toy\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","gf17_addition/experiments/toy/scripts/evaluate.py":"\"\"\"Print evaluation results for the gf17_addition toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n    correct = sum(g == r for g, r in zip(generated, references))\n    rate = correct / len(references) if references else 0.0\n    print(f\"Success rate: {100 * rate:.1f}%  ({correct}/{len(references)})\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","gf17_addition/experiments/toy/scripts/generate.py":"\"\"\"Generate the gf17_addition dataset for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\n\nfrom gf17_addition.core.generator import Gf17AdditionGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True) or {}\n    problem_generator = Gf17AdditionGenerator(**gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(cfg.dataset, instance_generator=problem_generator)\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","gf17_addition/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full gf17_addition/toy experiment end-to-end.\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\necho \"=== Step 1/3: Generate data ===\"\npython generate.py\necho \"=== Step 2/3: Train ===\"\npython train.py \"$@\"\necho \"=== Step 3/3: Evaluate ===\"\npython evaluate.py\n","gf17_addition/experiments/toy/scripts/train.py":"\"\"\"Train the gf17_addition model for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom gf17_addition.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(config_path: str | None, dryrun: bool) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"},"groebner_basis":{"groebner_basis/README.md":"# groebner_basis\n\n**Task**: Given a polynomial f and generators g1, g2, g3, predict the remainder\nof f on division by the Gröbner basis of the ideal ⟨g1, g2, g3⟩.\n\n## The math\n\nLet R = ℤ[x, y, z] and I = ⟨g1, g2, g3⟩ ⊂ R.\n\n1. Compute G = grevlex Gröbner basis of I  (using Buchberger's algorithm).\n2. Compute r = I.reduce(f)  (remainder of the division of f by G).\n\nThe model receives (f, G) as input and must predict r.\n\n**Why is this hard?** Even knowing G, computing r requires a multi-step\npolynomial division algorithm. The model must learn this algorithm implicitly.\n\n## Data\n\n- Field: ℤ (integers)\n- Variables: x, y, z\n- Polynomials: 2–5 terms, degree ≤ 4, coefficients in [-10, 10]\n- Stored as: pickle (SageMath objects)\n\n## Experiments\n\n| Experiment | What varies |\n|---|---|\n| `toy/` | Small model, 1000 train samples — verify the pipeline runs |\n| `scaling/` | Larger models and datasets — measure scaling behavior |\n| `ablation/` | Order (grevlex vs. lex), pattern (remainder vs. quotients+remainder) |\n\n## Quick start\n\n```bash\ncd groebner_basis/experiments/toy/scripts\npython generate.py          # generate data  (~1 min)\npython train.py             # train model    (~10 min on GPU)\npython evaluate.py          # evaluate       (prints success rate)\n```\n\nOr run everything:\n\n```bash\nbash run.sh\n```\n\n## Offline preprocessing (optional, recommended for `lex` order)\n\n`train.py` applies the `dataset_load_preprocessor` (TextToSage → FGLM for lex,\nTextToSage → ExpandedForm for C/E format) **every time it starts**. For large\ndatasets and the `lex` chain that means several minutes of SageMath/FGLM\nrecomputation before each run.\n\n`preprocess.py` runs that chain **once** offline and writes a JSONL cache. By\ndefault it also pre-tokenizes all the way to `input_ids` so that `train.py`\nperforms **zero tokenization at runtime** — only padding + GPU forward.\n\n```bash\ncd groebner_basis/experiments/toy/scripts\npython preprocess.py                          # cache (pretokenized) for degrevlex\npython preprocess.py --training_order lex     # cache (pretokenized) for lex order\npython preprocess.py --no-pretokenize         # cache stops at post-FGLM strings (legacy mode)\npython train.py --training_order lex          # auto-detects cache → fast startup\n```\n\nTwo cache flavors live side-by-side; `train.py` prefers the pre-tokenized one:\n\n| Cache directory | Contains | Skipped at training time |\n|---|---|---|\n| `processed_<order>_<format>_ids/` | `{\"input_ids\": [...], \"target_ids\": [...]}` | SageMath + UnifiedLexer + HF tokenizer |\n| `processed_<order>_<format>/`     | `{\"problem\": str, \"answer\": str}`         | SageMath only (lexer/tokenizer still online) |\n| (none)                            | raw `.txt`                                | nothing — legacy path |\n\nThe caches live next to your raw `.txt`:\n\n```\ndata/QQ/\n├── train_raw.txt\n├── test_raw.txt\n├── processed_lex_raw_ids/         ← pre-tokenized (default --pretokenize)\n│   ├── train_ids.jsonl            ← {\"input_ids\":[...], \"target_ids\":[...]}\n│   ├── test_ids.jsonl\n│   ├── _hash.txt                  ← includes tokenizer vocab in the hash\n│   └── _meta.yaml\n└── processed_lex_raw/             ← strings cache (--no-pretokenize)\n    ├── train_processed.jsonl\n    ├── test_processed.jsonl\n    ├── _hash.txt\n    └── _meta.yaml\n```\n\n**When to re-run `preprocess.py`** — any change to `lexer.yaml`, `data.yaml`\nsampler config, or the `--training_order` produces a new hash. `train.py`\ndetects the mismatch automatically and falls back to the slow path, printing\na `cache is STALE` warning. Re-run `preprocess.py` (or `--force`) to refresh.\n\n**When NOT to bother** — for `--training_order degrevlex` with the default raw\nlexer there is no load-time work to skip (the chain is empty), so the cache\njust copies the raw text. Still safe to use, but the speedup is zero.\n\n## User post-processing hooks (optional)\n\nTwo opt-in slots let you inject custom transformations of `(input_text, target_text)`\nafter the load chain (FGLM/Expanded/etc.) has run, before tokenization:\n\n| Slot | File (create to enable) | Scope |\n|---|---|---|\n| Base hook   | `shared/base_postprocessor.py`         | Applied to every task that wires user hooks (groebner + border) |\n| Per-task    | `groebner_basis/core/postprocessor.py` | Applied to groebner only, AFTER the base hook |\n\nEach file must expose:\n\n```python\ndef postprocess(input_text: str, target_text: str) -> tuple[str, str]:\n    return input_text, target_text  # default: identity\n```\n\nTo enable, copy the corresponding `.py.example` file:\n\n```bash\ncp shared/base_postprocessor.py.example shared/base_postprocessor.py\n# then edit shared/base_postprocessor.py\n```\n\nThe hook source code is folded into the preprocessing cache hash, so editing\n`postprocess()` automatically invalidates stale caches — your next\n`preprocess.py` run rebuilds them. If you delete the file the system reverts\nto identity (no hook).\n\nExamples and caveats are in [shared/base_postprocessor.py.example](../shared/base_postprocessor.py.example).\n\n## Variants\n\n- `--order lex` : convert to lex order via FGLM before training\n- `--pattern 2` : predict quotients + remainder instead of remainder only\n\n## Tokenization format (raw vs C/E expanded)\n\nTwo tokenization schemes are available. The training pipeline auto-detects which one\nto use from the `data.lexer_config` field in your `train.yaml`.\n\n| Format | Lexer config | How a term is encoded | Vocab size |\n|---|---|---|---|\n| **raw** (default) | `configs/lexer.yaml` | `\"x ^ 2 + y\"` → `[\"x\", \"^\", \"2\", \"+\", \"y\"]` | larger (one token per digit) |\n| **expanded** (C/E) | `configs/lexer_expanded.yaml` | `\"2*x^2*y\"` → `[\"C2\", \"E2\", \"E1\"]` | smaller, fixed |\n\n**Switching is one line**: in `experiments/<exp>/configs/train.yaml`, change\n\n```yaml\ndata:\n  lexer_config: ../configs/lexer.yaml           # raw  (default)\n  # lexer_config: ../configs/lexer_expanded.yaml  # C/E expanded\n```\n\nThat's it. `core/train.py` calls `detect_lexer_format()` on the file and wires\n`ExpandedFormLoadPreprocessor` into the load chain automatically when needed.\n\n**When to use which**:\n- **raw** — default, matches ISSAC '26 §5 reproduction. Handles coefficient swell on ℚ\n  via `digit_group: 1` (each digit is its own token). Use this to reproduce paper\n  results.\n- **expanded** — fixed-size vocab (only ranges `[Cmin, Cmax]` and `[Emin, Emax]`).\n  Useful if you want a smaller embedding table or to align with the\n  `polynomial_reduction` task in the ISSAC '26 paper. ⚠ Coefficients outside the\n  declared range produce OOV tokens — widen `coefficients` in `lexer_expanded.yaml`\n  if you change the field or sampler.\n\n**Caveat — combining with `--order lex`**: both modes work together. The lex\npreprocessor runs first (recomputes G in lex order via FGLM), then expanded form\nruns on the lex-ordered output. See [core/train.py](core/train.py) `run_training()`.\n","groebner_basis/core/__init__.py":"from .generator import GroebnerGenerator\nfrom .formatter import format_input, format_target\nfrom .parser import GroebnerLexOrderPreprocessor\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\n    \"GroebnerGenerator\",\n    \"format_input\",\n    \"format_target\",\n    \"GroebnerLexOrderPreprocessor\",\n    \"success_rate\",\n    \"run_training\",\n]\n","groebner_basis/core/formatter.py":"\"\"\"\nHelper functions to render SageMath polynomial objects as text strings.\n\nFormat\n------\nInput  : \"f1 | f2\"             (generators separated by ' | ')\nTarget : \"g1 | g2 | ... | gk\"  (Gröbner basis elements)\n\nThese are NOT used by the CALT DatasetPipeline (which calls str() on each\npolynomial automatically and joins lists with ' | '). They exist for:\n  - inspecting generated samples in notebooks/REPL\n  - building unit tests\n\"\"\"\n\nSEP = \" | \"\n\n\ndef format_input(generators: list) -> str:\n    \"\"\"[f1, f2] → 'f1 | f2'\"\"\"\n    return SEP.join(str(g) for g in generators)\n\n\ndef format_target(basis: list) -> str:\n    \"\"\"[g1, ..., gk] → 'g1 | ... | gk'\"\"\"\n    return SEP.join(str(g) for g in basis)\n","groebner_basis/core/generator.py":"\"\"\"\nGenerate (problem, answer) pairs for the Gröbner basis computation task.\n\nMath\n----\nR = k[x, y],  k ∈ {QQ, GF(7), ZZ}\nI = ⟨f1, f2⟩  (ideal generated by two polynomials)\n\nGiven f1, f2 ∈ R sampled at random:\n  - G = degrevlex Gröbner basis of I  (Buchberger's algorithm via SageMath)\n\nThe learning task is: given (f1, f2), predict G.\n\nReference\n---------\nMirrors `issac2026_experiments/groebner/generate_dataset.py` from the\nHiroshiKERA/calt branch experiment/issac2026.\n\"\"\"\n\nimport sage.all  # noqa: F401  # ensure Sage is initialised before any submodule import\nimport sage.misc.randstate as randstate  # type: ignore\nfrom sage.rings.rational_field import QQ  # type: ignore\n\nfrom calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\n\ndef _has_large_rational_coefficients(polynomials, threshold: int = 100) -> bool:\n    \"\"\"\n    Return True if any polynomial has a rational coefficient whose numerator OR\n    denominator has |value| ≥ threshold.\n\n    Used to reject samples with Buchberger coefficient swell over QQ.\n    For non-QQ fields (GF(p), ZZ) this check is silently skipped per coefficient.\n    \"\"\"\n    for p in polynomials:\n        for c in p.coefficients():\n            try:\n                num = c.numerator()\n                den = c.denominator()\n            except Exception:\n                continue\n            if abs(int(num)) >= threshold or abs(int(den)) >= threshold:\n                return True\n    return False\n\n\nclass GroebnerGenerator:\n    \"\"\"\n    Problem generator: F = [f1, ..., f_{num_polynomials}], answer = Gröbner basis of ⟨F⟩.\n\n    Returns SageMath polynomial objects; the CALT DatasetPipeline converts them\n    to strings via str() and joins with ' | '.\n\n    Parameters\n    ----------\n    sampler : PolynomialSampler\n        Builds the random polynomials in the chosen ring.\n    num_polynomials : int\n        Number of generators (default 2).\n    \"\"\"\n\n    def __init__(self, sampler: PolynomialSampler, num_polynomials: int = 2):\n        self.sampler = sampler\n        self.num_polynomials = num_polynomials\n\n    def __call__(self, seed: int) -> tuple[list, list]:\n        randstate.set_random_seed(seed)\n\n        R = self.sampler.get_ring()\n        is_QQ = R.base_ring() == QQ\n\n        # For QQ only: reject samples with rational coefficients ≥ 3 digits in numerator or denominator\n        max_retries = 100\n        last_F, last_G = None, None\n        for _ in range(max_retries):\n            F = self.sampler.sample(num_samples=self.num_polynomials)\n            ideal = R.ideal(F)\n            G = list(ideal.groebner_basis())\n            last_F, last_G = F, G\n\n            if not is_QQ:\n                break\n            if not _has_large_rational_coefficients(list(F) + list(G), threshold=100):\n                break\n\n        return last_F, last_G\n","groebner_basis/core/metrics.py":"\"\"\"\nMetrics for the Gröbner basis computation task.\n\nsuccess_rate() is called after training to report evaluation accuracy.\nExact string match: the model wins only when its output is character-for-character\nidentical to the ground-truth Gröbner basis string.\n\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"\n    Return the fraction of predictions that exactly match the target.\n\n    Exact string match (after stripping whitespace) is the standard metric\n    for this task — any difference in coefficients or ordering counts as failure.\n    \"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","groebner_basis/core/parser.py":"\"\"\"\nLoad-time preprocessor for lex order training of the Gröbner basis task.\n\nFor the default pipeline (degrevlex), no custom preprocessor is needed:\nCALT's text pipeline reads \"f1 | f2 # g1 | ... | gk\" directly.\n\nFor lex order training, the train.py wires up a ChainLoadPreprocessor:\n    TextToSageLoadPreprocessor  →  GroebnerLexOrderPreprocessor\n\nThe first stage parses text into SageMath polynomials in the (degrevlex) source ring.\nThe second stage converts F to a lex ring and recomputes the lex Gröbner basis.\n\nReference\n---------\nMirrors `_GroebnerLexOrderPreprocessor` from\nissac2026_experiments/groebner/train.py in HiroshiKERA/calt@experiment/issac2026.\n\"\"\"\n\nfrom typing import Any\n\nimport sage.all  # noqa: F401  # ensure Sage is initialised\nfrom sage.all import PolynomialRing  # type: ignore\n\n\nclass GroebnerLexOrderPreprocessor:\n    \"\"\"\n    Load-time converter: dict {'problem': [f1,f2,...], 'solution': [...]} →\n    (input_text, target_text) in lex order.\n\n    F and the lex Gröbner basis are joined with ' | '.\n    Requires a zero-dimensional ideal for the Gröbner basis to terminate.\n\n    Parameters\n    ----------\n    ring_src : PolynomialRing\n        The source ring used for generation (typically degrevlex).\n    delimiter : str\n        Separator character (default '|').\n    \"\"\"\n\n    def __init__(self, ring_src, delimiter: str = \"|\"):\n        self.R_src = ring_src\n        base = ring_src.base_ring()\n        names = ring_src.variable_names()\n        self.R_lex = PolynomialRing(base, names, order=\"lex\")\n        self.delimiter = delimiter\n\n    def process_sample(self, source: dict[str, Any]) -> tuple[str, str]:\n        if not isinstance(source, dict):\n            raise TypeError(\n                f\"GroebnerLexOrderPreprocessor expects dict source, got {type(source).__name__}\"\n            )\n        F_src = source.get(\"problem\") or []\n        F_lex = [self.R_lex(f) for f in F_src]\n        I_lex = self.R_lex.ideal(F_lex)\n        # Force `libsingular:std` instead of the default. The default tries\n        # `stdhilb`, which raises `intvec`/`bigintvec` on some QQ-lex inputs in\n        # SageMath 10.6. `libsingular:std` is the standard, stable choice for\n        # both QQ and GF(p).\n        G_lex = list(I_lex.groebner_basis(\"libsingular:std\"))\n\n        sep = f\" {self.delimiter} \"\n        input_text = sep.join(str(f) for f in F_lex)\n        target_text = sep.join(str(g) for g in G_lex)\n        return input_text, target_text\n","groebner_basis/core/train.py":"\"\"\"\nTraining runner for the Gröbner basis computation task.\n\nOrchestrates the four CALT pipelines:\n  1. IOPipeline      — load text data, tokenize with UnifiedLexer\n  2. ModelPipeline   — build the Transformer\n  3. TrainerPipeline — train (HuggingFace Trainer under the hood)\n  4. evaluate_and_save_generation — decode predictions, compute success rate\n\nFor `training_order=\"lex\"`, a ChainLoadPreprocessor (TextToSage → GroebnerLexOrder)\nis wired into the IOPipeline; it rebuilds F over a lex ring and recomputes\nthe Gröbner basis at load time.\n\nReference\n---------\nMirrors `issac2026_experiments/groebner/train.py` (HiroshiKERA/calt@experiment/issac2026).\n\"\"\"\n\nimport os\n\nimport sage.all  # noqa: F401  # initialise Sage before any submodule\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom calt.io import (\n    ChainLoadPreprocessor,\n    ExpandedFormLoadPreprocessor,\n    TextToSageLoadPreprocessor,\n)\nfrom calt.models import ModelPipeline\nfrom calt.trainer import TrainerPipeline, apply_dryrun_settings\n\nfrom shared.calt_adapter import (\n    build_io_pipeline_with_cache,\n    build_ring_from_sampler,\n    detect_lexer_format,\n)\n\nfrom .parser import GroebnerLexOrderPreprocessor\n\n\ndef build_load_preprocessor(\n    cfg: DictConfig,\n    data_cfg: DictConfig | None,\n    training_order: str = \"degrevlex\",\n):\n    \"\"\"\n    Build the load-time preprocessor chain that converts raw text data into the\n    string form expected by the tokenizer. Used by both run_training() and the\n    offline preprocess.py script so the two stay in sync.\n\n    Chain composition:\n      training_order=lex   → TextToSage + GroebnerLexOrder\n      lexer format=expanded → TextToSage + ExpandedForm (appended)\n\n    Returns\n    -------\n    tuple (preprocessor_or_None, format_str)\n        preprocessor is None when no preprocessing is needed (raw text + degrevlex).\n        format_str is \"raw\" or \"expanded\" for logging.\n    \"\"\"\n    lexer_format = detect_lexer_format(cfg.data.lexer_config)\n\n    # `lex` + `expanded` is currently unsupported: GroebnerLexOrderPreprocessor\n    # returns (str, str), but ExpandedFormLoadPreprocessor expects dict — they\n    # cannot be chained in that order. Stop early with a clear message rather\n    # than letting CALT raise a cryptic TypeError deep in the chain.\n    if training_order == \"lex\" and lexer_format == \"expanded\":\n        raise ValueError(\n            \"Unsupported configuration: training_order='lex' combined with \"\n            \"lexer format='expanded'.\\n\"\n            \"  Reason: GroebnerLexOrderPreprocessor returns (str, str) but \"\n            \"ExpandedFormLoadPreprocessor expects dict source. Chaining them \"\n            \"would crash at training time.\\n\"\n            \"  Pick ONE of these instead:\\n\"\n            \"    • training_order=lex      + lexer.yaml          (raw text)\\n\"\n            \"    • training_order=degrevlex + lexer_expanded.yaml (C/E format)\\n\"\n            \"    • training_order=degrevlex + lexer.yaml          (default)\"\n        )\n\n    needs_ring = (training_order == \"lex\") or (lexer_format == \"expanded\")\n    R_src = (\n        build_ring_from_sampler(data_cfg.sampler)\n        if (needs_ring and data_cfg is not None)\n        else None\n    )\n    if needs_ring and R_src is None:\n        raise ValueError(\n            \"Either training_order='lex' or lexer format='expanded' requires data_cfg \"\n            \"to rebuild the source ring.\"\n        )\n\n    chain = []\n    if training_order == \"lex\":\n        chain.append(TextToSageLoadPreprocessor(delimiter=\"|\", ring=R_src))\n        chain.append(GroebnerLexOrderPreprocessor(R_src, delimiter=\"|\"))\n    if lexer_format == \"expanded\":\n        if not chain:\n            chain.append(TextToSageLoadPreprocessor(delimiter=\"|\", ring=R_src))\n        chain.append(ExpandedFormLoadPreprocessor(delimiter=\" | \"))\n\n    # Wire optional user post-processing hooks (task 3).\n    # - shared/base_postprocessor.py   (applies to all tasks)\n    # - groebner_basis/core/postprocessor.py (per-task, runs after base)\n    # Both are opt-in: if neither file exists, this branch is a no-op.\n    from shared.user_postprocessor import (\n        get_user_postprocessors,\n        RawLineToTupleAdapter,\n        UserPostProcessorAdapter,\n    )\n    user_hooks = get_user_postprocessors(\"groebner_basis\")\n    if user_hooks:\n        # If no task chain step has run yet, parse the raw line first so the\n        # hooks receive a (str, str) tuple.\n        if not chain:\n            chain.append(RawLineToTupleAdapter())\n        chain.append(UserPostProcessorAdapter(user_hooks))\n\n    if not chain:\n        return None, lexer_format\n    if len(chain) == 1:\n        return chain[0], lexer_format\n    return ChainLoadPreprocessor(*chain), lexer_format\n\n\ndef run_training(\n    cfg: DictConfig,\n    data_cfg: DictConfig | None = None,\n    training_order: str = \"degrevlex\",\n    dryrun: bool = False,\n) -> float:\n    \"\"\"\n    Run the full training pipeline for the Gröbner basis task.\n\n    Parameters\n    ----------\n    cfg : DictConfig\n        Loaded train.yaml (must contain 'model', 'train', 'data' sections).\n    data_cfg : DictConfig | None\n        Loaded data.yaml (required when training_order='lex' to rebuild the ring).\n    training_order : \"degrevlex\" | \"lex\"\n        'degrevlex' uses the stored basis as-is.\n        'lex' converts F,G to lex order at load time and recomputes the basis.\n    dryrun : bool\n        Reduced settings for a fast smoke test.\n\n    Returns\n    -------\n    float\n        Exact-match success rate on the test set (between 0 and 1).\n    \"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    save_dir = save_dir.rstrip(\"/\") + f\"_{training_order}\"\n    cfg.train.save_dir = save_dir\n\n    if (\n        hasattr(cfg.train, \"wandb\")\n        and hasattr(cfg.train.wandb, \"name\")\n        and cfg.train.wandb.name\n    ):\n        cfg.train.wandb.name = f\"{cfg.train.wandb.name}_{training_order}\"\n\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    # Cache hierarchy (pretok → strings → raw fallback) lives in the CALT\n    # library now; build_io_pipeline_with_cache picks the most aggressive valid\n    # cache, wires load_preprocessor only on the raw path, and emits the\n    # cross-order diagnostic when nothing matches.\n    from shared.user_postprocessor import user_postprocessor_hash_contribution\n\n    hooks_hash = user_postprocessor_hash_contribution(\"groebner_basis\")\n\n    load_preprocessor, lexer_format = build_load_preprocessor(cfg, data_cfg, training_order)\n    io_pipeline, cache_kind = build_io_pipeline_with_cache(\n        cfg, data_cfg, training_order, lexer_format, load_preprocessor,\n        extra_hash_bytes=hooks_hash,\n    )\n    print(\n        f\"[run_training] cache={cache_kind} (format={lexer_format}, order={training_order})\"\n    )\n\n    io_dict = io_pipeline.build()\n\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","groebner_basis/experiments/ablation/README.md":"# Gröbner basis — ablation experiment\n\n**Goal**: study how training monomial order affects exact-match accuracy.\n\n| Knob | Choices |\n|------|---------|\n| `--training_order` | `degrevlex` (use stored basis as-is), `lex` (FGLM at load time) |\n\nLex bases are typically larger and have larger coefficients, so accuracy is\nexpected to drop vs degrevlex (see Table 5 of the ISSAC2026 paper: 42.4% degrevlex\nvs 32.4% lex on QQ).\n\n**Implementation note**: the lex Gröbner basis is computed at load time with\n`libsingular:std` explicitly (`core/parser.py`). The default `stdhilb` algorithm\nin SageMath 10.6 raises an `intvec` / `bigintvec` error on some QQ-lex inputs;\nforcing `libsingular:std` is the standard workaround.\n\n## How to run\n```bash\ncd groebner_basis/experiments/ablation/scripts\npython train.py --training_order degrevlex\npython train.py --training_order lex\n```\n\nReuses the dataset generated by the toy experiment (`../../toy/data/QQ/`).\nSave dirs are auto-suffixed with `_degrevlex` / `_lex` to avoid clobbering.\n","groebner_basis/experiments/ablation/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 6\n  num_encoder_heads: 8\n  num_decoder_layers: 6\n  num_decoder_heads: 8\n  d_model: 512\n  encoder_ffn_dim: 2048\n  decoder_ffn_dim: 2048\n  max_sequence_length: 2048\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0003\n  weight_decay: 0.0\n  warmup_ratio: 0.05\n  batch_size: 32\n  test_batch_size: 32\n  lr_scheduler_type: linear\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 4\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: groebner_basis_ablation\n    name: ablation_placeholder\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../../toy/data/QQ/train_raw.txt\n  test_dataset_path: ../../toy/data/QQ/test_raw.txt\n  lexer_config: ../../toy/configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","groebner_basis/experiments/ablation/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","groebner_basis/experiments/ablation/scripts/train.py":"\"\"\"Gröbner basis ablation: degrevlex vs lex training order.\n\nUsage: python train.py --training_order {degrevlex,lex}\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom groebner_basis.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--training_order\",\n    type=click.Choice([\"degrevlex\", \"lex\"]),\n    default=\"degrevlex\",\n)\n@click.option(\"--dryrun\", is_flag=True)\ndef main(training_order: str, dryrun: bool) -> None:\n    cfg = OmegaConf.load(config_dir(__file__) / \"train.yaml\")\n\n    data_cfg = None\n    if training_order == \"lex\":\n        # Reuse the toy data config so we know the source ring.\n        data_cfg = OmegaConf.load(config_dir(__file__).parent.parent / \"toy/configs/data.yaml\")\n\n    cfg.train.wandb.name = \"ablation\"\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, data_cfg=data_cfg, training_order=training_order, dryrun=dryrun)\n    print(f\"[{training_order}] Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n","groebner_basis/experiments/scaling/README.md":"# Gröbner basis — scaling experiment\n\n**Goal**: study how exact-match accuracy scales with model size and dataset size.\n\n| Knob | Choices |\n|------|---------|\n| `--model_size` | `small`, `medium`, `large` |\n| `--training_order` | `degrevlex` (default), `lex` (FGLM at load time) |\n\n## What this experiment varies vs `toy/`\n- Larger dataset: 50k train / 1k test (vs 1k / 200)\n- Harder polynomials: `max_degree=5`, `max_num_terms=6`\n- Configurable model size via CLI\n\n## How to run\n```bash\ncd groebner_basis/experiments/scaling/scripts\npython generate.py\npython train.py --model_size small\npython train.py --model_size large --training_order lex\n```\n","groebner_basis/experiments/scaling/configs/data.yaml":"# Groebner scaling: larger dataset and harder polynomials than toy/.\n\nsampler:\n  symbols: \"x,y\"\n  field_str: \"QQ\"\n  order: \"degrevlex\"\n  max_num_terms: 6\n  max_degree: 5\n  min_degree: 1\n  max_coeff: 5\n  degree_sampling: \"uniform\"\n  term_sampling: \"uniform\"\n\nproblem_generator:\n  num_polynomials: 2\n\ndataset:\n  save_dir: \"../data/QQ\"\n  num_train_samples: 50000\n  num_test_samples: 1000\n  batch_size: 1000\n  n_jobs: 8\n  root_seed: 42\n  verbose: true\n  backend: \"sagemath\"\n  save_text: true\n  save_json: false\n","groebner_basis/experiments/scaling/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 6\n  num_encoder_heads: 8\n  num_decoder_layers: 6\n  num_decoder_heads: 8\n  d_model: 512\n  encoder_ffn_dim: 2048\n  decoder_ffn_dim: 2048\n  max_sequence_length: 2048\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 200\n  learning_rate: 0.0003\n  weight_decay: 0.0\n  warmup_ratio: 0.05\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: linear\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 4\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: groebner_basis\n    name: scaling_large\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/QQ/train_raw.txt\n  test_dataset_path: ../data/QQ/test_raw.txt\n  lexer_config: ../../toy/configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","groebner_basis/experiments/scaling/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","groebner_basis/experiments/scaling/scripts/generate.py":"\"\"\"Generate the large-scale Gröbner basis dataset for the scaling experiment.\n\nReference: issac2026_experiments/groebner/generate_dataset.py.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\nfrom calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\nfrom groebner_basis.core.generator import GroebnerGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    sampler_cfg = dict(OmegaConf.to_container(cfg.sampler, resolve=True))\n    sampler = PolynomialSampler(**sampler_cfg)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True)\n    problem_generator = GroebnerGenerator(sampler=sampler, **gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(\n        cfg.dataset,\n        instance_generator=problem_generator,\n    )\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","groebner_basis/experiments/scaling/scripts/train.py":"\"\"\"Gröbner basis scaling: vary model size to study scaling behavior.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom groebner_basis.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\nMODEL_SIZES = {\n    \"small\":  dict(d_model=128, num_encoder_layers=2, num_decoder_layers=2,\n                   encoder_ffn_dim=512, decoder_ffn_dim=512,\n                   num_encoder_heads=4, num_decoder_heads=4),\n    \"medium\": dict(d_model=256, num_encoder_layers=4, num_decoder_layers=4,\n                   encoder_ffn_dim=1024, decoder_ffn_dim=1024,\n                   num_encoder_heads=8, num_decoder_heads=8),\n    \"large\":  dict(d_model=512, num_encoder_layers=6, num_decoder_layers=6,\n                   encoder_ffn_dim=2048, decoder_ffn_dim=2048,\n                   num_encoder_heads=8, num_decoder_heads=8),\n}\n\n\n@click.command()\n@click.option(\"--model_size\", type=click.Choice(list(MODEL_SIZES)), default=\"large\")\n@click.option(\"--training_order\", type=click.Choice([\"degrevlex\", \"lex\"]), default=\"degrevlex\")\n@click.option(\"--dryrun\", is_flag=True)\ndef main(model_size: str, training_order: str, dryrun: bool) -> None:\n    cfg = OmegaConf.load(config_dir(__file__) / \"train.yaml\")\n    cfg.model = OmegaConf.create({**OmegaConf.to_container(cfg.model), **MODEL_SIZES[model_size]})\n    cfg.train.wandb.name = f\"scaling_{model_size}\"\n\n    data_cfg = None\n    if training_order == \"lex\":\n        data_cfg = OmegaConf.load(config_dir(__file__) / \"data.yaml\")\n\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, data_cfg=data_cfg, training_order=training_order, dryrun=dryrun)\n    print(f\"[{model_size}/{training_order}] Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n","groebner_basis/experiments/toy/configs/data.yaml":"# Groebner task (toy): F=[f1,f2] over QQ[x,y] -> degrevlex Gröbner basis G of (F)\n\n# PolynomialSampler parameters (consumed by GroebnerGenerator)\nsampler:\n  symbols: \"x,y\"\n  field_str: \"QQ\"\n  order: \"degrevlex\"\n  max_num_terms: 5\n  max_degree: 4\n  min_degree: 1\n  max_coeff: 5\n  degree_sampling: \"uniform\"\n  term_sampling: \"uniform\"\n\n# GroebnerGenerator-specific parameters\nproblem_generator:\n  num_polynomials: 2\n\n# DatasetPipeline parameters\ndataset:\n  save_dir: \"../data/QQ\"\n  num_train_samples: 1000\n  num_test_samples: 200\n  batch_size: 100\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sagemath\"\n  save_text: true\n  save_json: false\n","groebner_basis/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", -100, 100]\n  misc: [\"+\", \"-\", \"*\", \"^\", \"x\", \"y\", \"|\", \"/\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 1        # tokenize each digit separately → handles any coefficient size\n  allow_float: false\n\nstrict: true\ninclude_base_vocab: true\n","groebner_basis/experiments/toy/configs/lexer_expanded.yaml":"# Lexer EXPANDED — C/E form for the Gröbner basis task.\n#\n# Each monomial is tokenized as:\n#   C<coeff> E<exp_x> E<exp_y>     for one term in 2 variables (x, y)\n# Polynomials within a sample are separated by ' | '.\n# Terms within a polynomial are separated by ' + ' (sign attached to the coefficient).\n#\n# Example: \"2*x^2*y + 5*x - 7\" → \"C2 E2 E1 + C5 E1 E0 + C-7 E0 E0\"\n#\n# To use this format instead of the default raw text format, point train.yaml's\n# data.lexer_config to this file instead of lexer.yaml.\n#\n# Reference: ISSAC '26 paper §2.2 + issac2026_experiments/polynomial_reduction/configs/.\n\nvocab:\n  range:\n    coefficients: [\"C\", -99, 99]  # coefficient range (must cover everything you see)\n    exponents:    [\"E\", 0, 5]     # exponent per variable (max_degree=4 in our config, with safety margin)\n  misc: [\"+\", \"|\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 0\n  allow_float: false\n\nstrict: true\ninclude_base_vocab: true\n","groebner_basis/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 3\n  num_encoder_heads: 4\n  num_decoder_layers: 3\n  num_decoder_heads: 4\n  d_model: 256\n  encoder_ffn_dim: 1024\n  decoder_ffn_dim: 1024\n  max_sequence_length: 2048\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 50\n  learning_rate: 0.0003\n  weight_decay: 0.0\n  warmup_ratio: 0.05\n  batch_size: 16\n  test_batch_size: 16\n  lr_scheduler_type: linear\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: groebner_basis\n    name: toy_QQ\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/QQ/train_raw.txt\n  test_dataset_path: ../data/QQ/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","groebner_basis/experiments/toy/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%  ({sum(g == r for g, r in zip(generated, references))}/{len(references)})\")\n\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","groebner_basis/experiments/toy/scripts/generate.py":"\"\"\"Generate the Gröbner basis dataset for the toy experiment.\n\nReference: issac2026_experiments/groebner/generate_dataset.py.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\nfrom calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\nfrom groebner_basis.core.generator import GroebnerGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML (defaults to ../configs/data.yaml).\",\n)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    sampler_cfg = dict(OmegaConf.to_container(cfg.sampler, resolve=True))\n    sampler = PolynomialSampler(**sampler_cfg)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True)\n    problem_generator = GroebnerGenerator(sampler=sampler, **gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(\n        cfg.dataset,\n        instance_generator=problem_generator,\n    )\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","groebner_basis/experiments/toy/scripts/preprocess.py":"\"\"\"Materialize a JSONL preprocessing cache for the toy Gröbner experiment.\n\nRuns the load-time chain (TextToSage / FGLM-lex / ExpandedForm — whichever applies\nto your training_order + lexer.yaml) ONCE over train_raw.txt + test_raw.txt and\nwrites the result to `<data>/processed_<order>_<format>/`. Training will detect\nthe cache automatically and skip the SageMath/FGLM step on subsequent runs.\n\nRe-run this script whenever you change lexer.yaml, data.yaml's sampler, or the\n--training_order. The cache is hash-keyed so a stale cache is detected and\nignored (train.py falls back to raw + on-the-fly preprocessing).\n\nUsage\n-----\n    cd groebner_basis/experiments/toy/scripts\n    python preprocess.py                          # degrevlex + lexer from train.yaml\n    python preprocess.py --training_order lex     # build the lex cache\n    python preprocess.py --force                  # rebuild even if hash matches\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom groebner_basis.core.train import build_load_preprocessor\nfrom shared.calt_adapter import preprocess_to_ids, run_preprocess\nfrom shared.paths import config_dir\nfrom shared.user_postprocessor import user_postprocessor_hash_contribution\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to train config YAML (defaults to ../configs/train.yaml).\",\n)\n@click.option(\n    \"--data_config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML (defaults to ../configs/data.yaml).\",\n)\n@click.option(\n    \"--training_order\",\n    type=click.Choice([\"degrevlex\", \"lex\"]),\n    default=\"degrevlex\",\n    help=\"Monomial order: must match what you'll pass to train.py.\",\n)\n@click.option(\"--force\", is_flag=True, help=\"Rebuild even if hash matches.\")\n@click.option(\n    \"--pretokenize/--no-pretokenize\",\n    default=True,\n    help=\"Pre-tokenize all the way to input_ids (default ON). \"\n         \"OFF stores post-load-preprocessor strings only; training still tokenizes online.\",\n)\ndef main(\n    config_path: str | None,\n    data_config_path: str | None,\n    training_order: str,\n    force: bool,\n    pretokenize: bool,\n) -> None:\n    \"\"\"Preprocess raw .txt → JSONL cache for fast training startup.\"\"\"\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n\n    data_path = Path(data_config_path) if data_config_path else config_dir(__file__) / \"data.yaml\"\n    data_cfg = OmegaConf.load(data_path)\n\n    load_preprocessor, lexer_format = build_load_preprocessor(cfg, data_cfg, training_order)\n    hooks_hash = user_postprocessor_hash_contribution(\"groebner_basis\")\n    if pretokenize:\n        cache_dir = preprocess_to_ids(\n            cfg, data_cfg, training_order, lexer_format, load_preprocessor,\n            force=force, extra_hash_bytes=hooks_hash,\n        )\n    else:\n        cache_dir = run_preprocess(\n            cfg, data_cfg, training_order, lexer_format, load_preprocessor,\n            force=force, extra_hash_bytes=hooks_hash,\n        )\n    print(f\"[done] cache at: {cache_dir}\")\n\n\nif __name__ == \"__main__\":\n    main()\n","groebner_basis/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full groebner_basis/toy experiment end-to-end.\n#\n# Usage:\n#   bash run.sh                                  # defaults: degrevlex, raw, with pretok cache\n#   bash run.sh --order lex                      # use lex order (FGLM)\n#   bash run.sh --lexer expanded                 # use C/E expanded vocab\n#   bash run.sh --order lex --lexer expanded     # combine\n#   bash run.sh --no-preprocess                  # skip preprocess.py (legacy training path)\n#   bash run.sh --dryrun                         # fast smoke test\n#   bash run.sh --force                          # rebuild caches even if hash matches\n#\n# What this script does (in order):\n#   1. python generate.py             # raw text data from SageMath (if not already done)\n#   2. python preprocess.py [flags]   # build the pretokenized cache (unless --no-preprocess)\n#   3. python train.py [flags]        # train + evaluate (cache picked up automatically)\n\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\n\n# ------------- Preflight: check the right Python is on PATH --------------\n# We need calt-x AND sage installed. If `python -c 'import calt, sage'` fails,\n# the user is on the wrong env (typical: base conda instead of calt-env).\nPY=\"${PYTHON:-python}\"\nif ! \"$PY\" -c \"import calt, sage\" >/dev/null 2>&1; then\n    echo \"❌ Wrong Python environment: \\`$PY\\` cannot import \\`calt\\` or \\`sage\\`.\" >&2\n    echo \"   Fix one of these:\" >&2\n    echo \"     1. Activate the conda env:  conda activate calt-env\" >&2\n    echo \"     2. Or set PYTHON explicitly: PYTHON=/path/to/calt-env/bin/python bash run.sh\" >&2\n    echo \"\" >&2\n    echo \"   Looking for ANY env that has calt installed...\" >&2\n    for cand in /home/t-maxime/miniconda3/envs/*/bin/python /opt/conda/envs/*/bin/python; do\n        if [[ -x \"$cand\" ]] && \"$cand\" -c \"import calt\" >/dev/null 2>&1; then\n            echo \"   Found: $cand\" >&2\n            echo \"     → re-run with: PYTHON=$cand bash run.sh ${ORIGINAL_ARGS:-}\" >&2\n            break\n        fi\n    done\n    exit 1\nfi\n# From here on, all python invocations use $PY.\n\n# ------------- Parse flags -----------------------------------------------\nORDER=\"degrevlex\"\nLEXER=\"raw\"\nPREPROCESS=1\nDRYRUN=\"\"\nFORCE=\"\"\nwhile [[ $# -gt 0 ]]; do\n    case $1 in\n        --order)            ORDER=\"$2\"; shift 2 ;;\n        --lexer)            LEXER=\"$2\"; shift 2 ;;\n        --no-preprocess)    PREPROCESS=0; shift ;;\n        --dryrun)           DRYRUN=\"--dryrun\"; shift ;;\n        --force)            FORCE=\"--force\"; shift ;;\n        -h|--help)\n            sed -n '2,18p' \"$0\" | sed 's/^# \\?//'\n            exit 0\n            ;;\n        *) echo \"Unknown flag: $1\" >&2; exit 2 ;;\n    esac\ndone\n\n# ------------- Validate flags --------------------------------------------\ncase $ORDER in\n    degrevlex|lex) ;;\n    *) echo \"Invalid --order $ORDER (must be degrevlex or lex)\" >&2; exit 2 ;;\nesac\ncase $LEXER in\n    raw|expanded) ;;\n    *) echo \"Invalid --lexer $LEXER (must be raw or expanded)\" >&2; exit 2 ;;\nesac\n\n# ------------- Plan -----------------------------------------------------\nLEXER_PATH=\"../configs/lexer.yaml\"\n[[ \"$LEXER\" == \"expanded\" ]] && LEXER_PATH=\"../configs/lexer_expanded.yaml\"\n\necho \"=================================================================\"\necho \"  groebner_basis/toy — end-to-end run\"\necho \"  order   : $ORDER\"\necho \"  lexer   : $LEXER  ($LEXER_PATH)\"\necho \"  preproc : $([ $PREPROCESS -eq 1 ] && echo enabled || echo skipped)\"\necho \"  dryrun  : $([ -n \"$DRYRUN\" ] && echo YES || echo no)\"\necho \"=================================================================\"\n\n# ------------- Apply lexer override --------------------------------------\n# train.yaml & data.yaml live in ../configs. We don't modify them on disk;\n# instead we generate a temp train.yaml with the lexer override.\nTMP_TRAIN_YAML=\"$(mktemp --suffix=.yaml)\"\ntrap 'rm -f \"$TMP_TRAIN_YAML\"' EXIT\nsed \"s|lexer_config:.*|lexer_config: $LEXER_PATH|\" ../configs/train.yaml > \"$TMP_TRAIN_YAML\"\n\n# ------------- Step 1: generate ------------------------------------------\nif [[ ! -f ../data/QQ/train_raw.txt ]]; then\n    echo \"=== Step 1/3: Generate data ===\"\n    \"$PY\" generate.py\nelse\n    echo \"=== Step 1/3: Generate data ===\"\n    echo \"    ../data/QQ/train_raw.txt already exists, skipping (delete to regenerate)\"\nfi\n\n# ------------- Step 2: preprocess (optional) -----------------------------\nif [[ $PREPROCESS -eq 1 ]]; then\n    echo \"=== Step 2/3: Preprocess (build cache) ===\"\n    \"$PY\" preprocess.py \\\n        --config_path \"$TMP_TRAIN_YAML\" \\\n        --training_order \"$ORDER\" \\\n        $FORCE\nelse\n    echo \"=== Step 2/3: Preprocess SKIPPED (--no-preprocess) ===\"\nfi\n\n# ------------- Step 3: train + evaluate ----------------------------------\necho \"=== Step 3/3: Train + Evaluate ===\"\n\"$PY\" train.py \\\n    --config_path \"$TMP_TRAIN_YAML\" \\\n    --training_order \"$ORDER\" \\\n    $DRYRUN\n\necho \"=== Step 4/3: Evaluate (optional, results already printed by train) ===\"\n\"$PY\" evaluate.py --training_order \"$ORDER\" || echo \"(evaluate.py is optional — train.py already wrote eval_results/)\"\n\necho \"=================================================================\"\necho \"  DONE. Output: ../outputs/results_${ORDER}/\"\necho \"=================================================================\"\n","groebner_basis/experiments/toy/scripts/train.py":"\"\"\"Train the Gröbner basis model for the toy experiment.\n\nReference: issac2026_experiments/groebner/train.py.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom groebner_basis.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to train config YAML (defaults to ../configs/train.yaml).\",\n)\n@click.option(\n    \"--data_config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML (defaults to ../configs/data.yaml). Used for lex training_order.\",\n)\n@click.option(\n    \"--training_order\",\n    type=click.Choice([\"degrevlex\", \"lex\"]),\n    default=\"degrevlex\",\n    help=\"Monomial order: degrevlex (default) or lex (FGLM at load time).\",\n)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(\n    config_path: str | None,\n    data_config_path: str | None,\n    training_order: str,\n    dryrun: bool,\n) -> None:\n    \"\"\"Train a Transformer to compute Gröbner bases from polynomial generators.\"\"\"\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n\n    # Always load data.yaml when it exists. The sampler dict participates in\n    # the cache hash (compute_*_hash), so it must be passed identically to\n    # preprocess.py and train.py — otherwise hashes mismatch and a freshly\n    # built cache is detected as STALE on the very next training run.\n    data_path = Path(data_config_path) if data_config_path else config_dir(__file__) / \"data.yaml\"\n    data_cfg = OmegaConf.load(data_path) if data_path.exists() else None\n\n    success = run_training(cfg, data_cfg=data_cfg, training_order=training_order, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"},"integer_factorization":{"integer_factorization/README.md":"# integer_factorization\n\n**Task**: Recover the prime factors whose product is a given integer.\n\n*Adapted from the official [calt-codebase](https://github.com/HiroshiKERA/calt-codebase) examples.*\n\n## Data format\n\n```\nInput  : 2310\nOutput : 2 | 3 | 5 | 7 | 11\n```\n\n## Quick start\n\n```bash\ncd integer_factorization/experiments/toy/scripts\npython generate.py    # 1. build the dataset  -> ../data/\npython train.py       # 2. train the model    -> ../outputs/results/\npython evaluate.py    # 3. report the success rate\n```\n\nOr `bash run.sh` (add `--dryrun` for a quick smoke test).\n","integer_factorization/core/__init__.py":"from .generator import IntegerFactorizationGenerator\nfrom .formatter import format_input, format_target\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\"IntegerFactorizationGenerator\", \"format_input\", \"format_target\", \"success_rate\", \"run_training\"]\n","integer_factorization/core/formatter.py":"\"\"\"String format for the integer_factorization task.\n\nInput  : 2310\nOutput : 2 | 3 | 5 | 7 | 11\n\nThe generator already returns ready-to-write strings; these helpers mirror that.\n\"\"\"\n\n\ndef format_input(problem) -> str:\n    return str(problem)\n\n\ndef format_target(answer) -> str:\n    return str(answer)\n","integer_factorization/core/generator.py":"\"\"\"Generate (problem, answer) pairs for the integer factorization task.\n\nAdapted from HiroshiKERA/calt-codebase examples/integer_factorization.\nInput  : the product of `n_primes` distinct primes (each <= max_number)\nOutput : those primes, sorted, joined by \" | \"  (e.g. \"2310\" -> \"2 | 3 | 5 | 7 | 11\")\n\"\"\"\n\nimport math\nimport random\n\n\nclass IntegerFactorizationGenerator:\n    def __init__(self, n_primes: int = 5, max_number: int = 30):\n        self.n_primes = n_primes\n        self.max_number = max_number\n        self._primes = self._sieve(max_number)\n        if len(self._primes) < n_primes:\n            raise ValueError(\"max_number too small for n_primes distinct primes\")\n\n    def __call__(self, seed: int) -> tuple[str, str]:\n        random.seed(seed)\n        sampled = random.sample(self._primes, self.n_primes)\n        product = math.prod(sampled)\n        return str(product), \" | \".join(str(p) for p in sorted(sampled))\n\n    @staticmethod\n    def _sieve(n: int) -> list[int]:\n        flags = [True] * (n + 1)\n        flags[0] = flags[1] = False\n        for i in range(2, int(n**0.5) + 1):\n            if flags[i]:\n                for j in range(i * i, n + 1, i):\n                    flags[j] = False\n        return [i for i in range(2, n + 1) if flags[i]]\n","integer_factorization/core/metrics.py":"\"\"\"Metrics for the integer_factorization task.\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"Fraction of predictions that exactly match the target (after .strip()).\"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","integer_factorization/core/parser.py":"\"\"\"Load-time preprocessor for the integer_factorization task.\n\nPlain text (\"input # output\" per line) is read directly by CALT's default text\nloader — no custom preprocessor is needed.\n\"\"\"\n","integer_factorization/core/train.py":"\"\"\"Training runner for the integer_factorization task.\n\nPlain-text storage means CALT's default text loader handles parsing, so no\ncustom load_preprocessor is needed (same simple pipeline as the parity task).\n\"\"\"\n\nimport os\n\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom shared.calt_adapter import (\n    IOPipeline,\n    ModelPipeline,\n    TrainerPipeline,\n    apply_dryrun_settings,\n)\n\n\ndef run_training(cfg: DictConfig, dryrun: bool = False) -> float:\n    \"\"\"Run the full pipeline (load -> build -> train -> evaluate).\"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    io_dict = IOPipeline.from_config(cfg.data).build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","integer_factorization/experiments/toy/configs/data.yaml":"# integer_factorization task (toy) — adapted from HiroshiKERA/calt-codebase examples/integer_factorization\n\nproblem_generator:\n  n_primes: 5\n  max_number: 30\n\ndataset:\n  save_dir: \"../data\"\n  num_train_samples: 5000\n  num_test_samples: 500\n  batch_size: 1000\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sympy\"\n  save_text: true\n  save_json: false\n","integer_factorization/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", 0, 1000]\n  misc: [\"|\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: false\n  digit_group: 3\n  allow_float: false\n\nstrict: false\ninclude_base_vocab: true\n","integer_factorization/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 96\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: integer_factorization\n    name: toy\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","integer_factorization/experiments/toy/scripts/evaluate.py":"\"\"\"Print evaluation results for the integer_factorization toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n    correct = sum(g == r for g, r in zip(generated, references))\n    rate = correct / len(references) if references else 0.0\n    print(f\"Success rate: {100 * rate:.1f}%  ({correct}/{len(references)})\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","integer_factorization/experiments/toy/scripts/generate.py":"\"\"\"Generate the integer_factorization dataset for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\n\nfrom integer_factorization.core.generator import IntegerFactorizationGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True) or {}\n    problem_generator = IntegerFactorizationGenerator(**gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(cfg.dataset, instance_generator=problem_generator)\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","integer_factorization/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full integer_factorization/toy experiment end-to-end.\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\necho \"=== Step 1/3: Generate data ===\"\npython generate.py\necho \"=== Step 2/3: Train ===\"\npython train.py \"$@\"\necho \"=== Step 3/3: Evaluate ===\"\npython evaluate.py\n","integer_factorization/experiments/toy/scripts/train.py":"\"\"\"Train the integer_factorization model for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom integer_factorization.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(config_path: str | None, dryrun: bool) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"},"parity":{"parity/README.md":"# parity\n\n**Task**: Given a permutation σ of {0, 1, …, n−1}, predict its sign: +1 (even) or −1 (odd).\n\n## The math\n\nA permutation σ is **even** if it can be written as a product of an even number\nof transpositions, and **odd** otherwise.\n\nEquivalently:\n\n    sign(σ) = (−1)^(number of inversions)\n\nwhere an **inversion** is a pair (i, j) with i < j but σ(i) > σ(j).\n\n**Example**:\n\n    σ = [2, 0, 1]   (σ(0)=2, σ(1)=0, σ(2)=1)\n    Inversions: (0,1) since σ(0)=2 > σ(1)=0\n                (0,2) since σ(0)=2 > σ(2)=1\n    2 inversions → even → sign(σ) = +1\n\n## Why this task?\n\nParity is a benchmark for learning **global** properties of sequences:\nthe model cannot look at any single element — it must compare all pairs.\nThis is known to require attention across the full sequence length.\n\n## Data format\n\nInput  : space-separated integers — the one-line notation of σ\nOutput : \"+1\" or \"−1\"\n\n    \"2 0 1\"  →  \"+1\"\n    \"3 0 2 1\"  →  \"−1\"\n\n## Experiments\n\n| Experiment | What varies |\n|---|---|\n| `toy/` | n=5 permutations, 1000 samples — verify the pipeline |\n| `scaling/` | n ∈ {5, 8, 12} — does performance degrade with longer sequences? |\n\n## Quick start\n\n```bash\ncd parity/experiments/toy/scripts\npython generate.py\npython train.py\npython evaluate.py\n```\n","parity/core/__init__.py":"from .generator import ParityGenerator, count_inversions, permutation_parity\nfrom .formatter import format_input, format_target\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\n    \"ParityGenerator\",\n    \"count_inversions\",\n    \"permutation_parity\",\n    \"format_input\",\n    \"format_target\",\n    \"success_rate\",\n    \"run_training\",\n]\n","parity/core/formatter.py":"\"\"\"\nConvert permutation objects to their string representations.\n\nFormat\n------\nInput  : space-separated integers — the one-line notation of σ\nOutput : \"+1\" (even) or \"-1\" (odd)\n\nExample:\n    format_input([2, 0, 1])   →  \"2 0 1\"\n    format_target(+1)         →  \"+1\"\n    format_target(-1)         →  \"-1\"\n\nWhy these tokens?\n-----------------\n\"+1\" and \"-1\" are single tokens in the CALT vocabulary (numbers with sign).\nThe model output space is effectively binary, which makes evaluation simple.\n\"\"\"\n\n\ndef format_input(perm: list[int]) -> str:\n    \"\"\"\n    Convert a permutation list to a space-separated string.\n\n    Example:  [2, 0, 1]  →  \"2 0 1\"\n    \"\"\"\n    return \" \".join(str(x) for x in perm)\n\n\ndef format_target(parity: int) -> str:\n    \"\"\"\n    Convert +1 / −1 parity to its string token.\n\n    Example:  +1  →  \"+1\"    −1  →  \"-1\"\n    \"\"\"\n    return \"+1\" if parity == 1 else \"-1\"\n","parity/core/generator.py":"\"\"\"\nGenerate (problem, answer) pairs for the permutation parity task.\n\nMath\n----\nInput  : a random permutation σ of {0, 1, …, n−1}\nOutput : sign(σ) ∈ {+1, −1}\n\nsign(σ) = (−1)^k   where k = number of inversions in σ\n\nAn inversion is a pair (i, j) with i < j and σ(i) > σ(j).\n\nOutput format\n-------------\nThe generator returns plain strings. The DatasetPipeline writes them to disk:\n  - input  : \"2 0 1\"        (space-separated one-line notation)\n  - output : \"+1\" or \"-1\"\n\nReference\n---------\nClass-based generator pattern matching\nissac2026_experiments/digit_product/generate_dataset.py\n(HiroshiKERA/calt@experiment/issac2026).\n\"\"\"\n\nimport random\nfrom itertools import combinations\n\n\ndef count_inversions(perm: list[int]) -> int:\n    \"\"\"Number of pairs (i, j) with i < j and perm[i] > perm[j].\"\"\"\n    return sum(\n        1 for i, j in combinations(range(len(perm)), 2)\n        if perm[i] > perm[j]\n    )\n\n\ndef permutation_parity(perm: list[int]) -> int:\n    \"\"\"+1 (even) or −1 (odd) — equals (−1)^(number of inversions).\"\"\"\n    return 1 if count_inversions(perm) % 2 == 0 else -1\n\n\nclass ParityGenerator:\n    \"\"\"\n    Problem generator: random permutation σ of {0,…,n−1}, answer is sign(σ).\n\n    Parameters\n    ----------\n    n : int\n        Permutation length (default 5).\n    \"\"\"\n\n    def __init__(self, n: int = 5):\n        if n < 1:\n            raise ValueError(f\"n must be ≥ 1, got {n}\")\n        self.n = n\n\n    def __call__(self, seed: int) -> tuple[str, str]:\n        random.seed(seed)\n        perm = list(range(self.n))\n        random.shuffle(perm)\n        parity = permutation_parity(perm)\n        input_str = \" \".join(str(x) for x in perm)\n        target_str = \"+1\" if parity == 1 else \"-1\"\n        return input_str, target_str\n","parity/core/metrics.py":"\"\"\"\nMetrics for the parity task.\n\nsuccess_rate() is the exact-match accuracy used after training to score the model.\n\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"\n    Return the fraction of predictions that exactly match the target parity.\n\n    Both \"+1\" and \"-1\" are single tokens — exact match is the right metric.\n    \"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","parity/core/parser.py":"\"\"\"\nLoad-time preprocessor for the parity task.\n\nThe default text pipeline reads \"2 0 1 # +1\" directly — no custom preprocessor is needed.\nThis module is reserved for future extensions.\n\"\"\"\n","parity/core/train.py":"\"\"\"\nTraining runner for the parity task.\n\nThe parity task uses plain-text storage (no pickle), so no custom\nload_preprocessor is needed — CALT's TextDefaultLoadPreprocessor handles it.\n\"\"\"\n\nimport os\n\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom shared.calt_adapter import (\n    IOPipeline,\n    ModelPipeline,\n    TrainerPipeline,\n    apply_dryrun_settings,\n)\n\n\ndef run_training(cfg: DictConfig, dryrun: bool = False) -> float:\n    \"\"\"\n    Run the full training pipeline for the permutation parity task.\n\n    Parameters\n    ----------\n    cfg : DictConfig\n        Config with 'model', 'train', 'data' sections.\n    dryrun : bool\n        If True, run for 1 epoch on a tiny subset.\n\n    Returns\n    -------\n    float\n        Exact-match success rate on the test set.\n    \"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    # No custom preprocessor: text format is handled automatically\n    io_dict = IOPipeline.from_config(cfg.data).build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","parity/experiments/ablation/README.md":"# Parity — ablation experiment\n\n**Goal**: study which model knobs matter for permutation parity.\n\nVary one knob at a time over the toy dataset (`n=5`):\n\n| Knob | Choices |\n|------|---------|\n| `--model_size` | `small`, `medium`, `large` |\n\n## How to run\n```bash\ncd parity/experiments/ablation/scripts\n# Reuses the dataset generated by the toy experiment\npython train.py --model_size small\npython train.py --model_size large\n```\n\nOutputs land in `outputs/results_<model_size>/` so successive runs do not overwrite.\n","parity/experiments/ablation/configs/train.yaml":"# Parity ablation — train.yaml is the base; --model_size CLI overrides model/*.\n\nmodel:\n  model_type: generic\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 64\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: parity_ablation\n    name: ablation\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../../toy/data/train_raw.txt\n  test_dataset_path: ../../toy/data/test_raw.txt\n  lexer_config: ../../toy/configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","parity/experiments/ablation/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","parity/experiments/ablation/scripts/train.py":"\"\"\"Ablation: vary the model size for the parity task.\n\nUsage: python train.py --model_size {small,medium,large}\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom parity.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\nMODEL_SIZES = {\n    \"small\":  dict(d_model=64,  num_encoder_layers=2, num_decoder_layers=2,\n                   encoder_ffn_dim=256, decoder_ffn_dim=256,\n                   num_encoder_heads=4, num_decoder_heads=4),\n    \"medium\": dict(d_model=128, num_encoder_layers=4, num_decoder_layers=4,\n                   encoder_ffn_dim=512, decoder_ffn_dim=512,\n                   num_encoder_heads=8, num_decoder_heads=8),\n    \"large\":  dict(d_model=256, num_encoder_layers=6, num_decoder_layers=6,\n                   encoder_ffn_dim=1024, decoder_ffn_dim=1024,\n                   num_encoder_heads=8, num_decoder_heads=8),\n}\n\n\n@click.command()\n@click.option(\"--model_size\", type=click.Choice(list(MODEL_SIZES)), default=\"small\")\n@click.option(\"--dryrun\", is_flag=True)\ndef main(model_size: str, dryrun: bool) -> None:\n    cfg = OmegaConf.load(config_dir(__file__) / \"train.yaml\")\n    overrides = MODEL_SIZES[model_size]\n    cfg.model = OmegaConf.create({**OmegaConf.to_container(cfg.model), **overrides})\n    cfg.train.save_dir = f\"../outputs/results_{model_size}\"\n    cfg.train.wandb.name = f\"ablation_{model_size}\"\n\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"[{model_size}] Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n","parity/experiments/scaling/README.md":"# Parity — scaling experiment\n\n**Goal**: study how exact-match accuracy scales with the permutation length `n`\n(harder for larger `n` because the input space is `n!`).\n\n## What it varies vs `toy/`\n- Larger dataset (50k train / 1k test)\n- `n` is selectable via CLI flag: `--n {5,7,10}`\n- Larger model\n\n## How to run\n```bash\ncd parity/experiments/scaling/scripts\npython generate.py --n 7         # generates ../data_n7/\npython train.py --n 7            # trains on it\n```\n\nThe default toy run lives in `../toy/` and uses `n=5`.\n","parity/experiments/scaling/configs/data.yaml":"# Parity scaling: larger dataset, permutation length controlled by --n CLI flag.\n\nproblem_generator:\n  n: 7\n\ndataset:\n  save_dir: \"../data\"        # generate.py overrides to \"../data_n{N}\"\n  num_train_samples: 50000\n  num_test_samples: 1000\n  batch_size: 5000\n  n_jobs: 8\n  root_seed: 42\n  verbose: true\n  backend: \"sympy\"\n  save_text: true\n  save_json: false\n","parity/experiments/scaling/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 4\n  num_encoder_heads: 8\n  num_decoder_layers: 4\n  num_decoder_heads: 8\n  d_model: 256\n  encoder_ffn_dim: 1024\n  decoder_ffn_dim: 1024\n  max_sequence_length: 128\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 200\n  learning_rate: 0.0003\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 256\n  test_batch_size: 256\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 4\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: parity\n    name: scaling_n7\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../../toy/configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","parity/experiments/scaling/scripts/evaluate.py":"\"\"\"Load a trained model and evaluate it on the test set.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%  ({sum(g == r for g, r in zip(generated, references))}/{len(references)})\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","parity/experiments/scaling/scripts/generate.py":"\"\"\"Generate the parity scaling dataset.\n\nUsage: python generate.py --n 7\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\n\nfrom parity.core.generator import ParityGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--n\", type=int, default=None, help=\"Permutation length (overrides config).\")\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML.\",\n)\ndef main(n: int | None, config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    if n is not None:\n        cfg.problem_generator.n = n\n    cfg.dataset.save_dir = f\"../data_n{cfg.problem_generator.n}\"\n\n    set_seed(cfg.dataset.root_seed)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True)\n    problem_generator = ParityGenerator(**gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(\n        cfg.dataset,\n        instance_generator=problem_generator,\n    )\n    pipeline.run()\n    print(f\"Dataset generation completed (n={cfg.problem_generator.n})\")\n\n\nif __name__ == \"__main__\":\n    main()\n","parity/experiments/scaling/scripts/train.py":"\"\"\"Train the parity model at a chosen permutation length.\n\nUsage: python train.py --n 7\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom parity.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--n\", type=int, default=None, help=\"Permutation length to train on.\")\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to train config YAML.\",\n)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(n: int | None, config_path: str | None, dryrun: bool) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n\n    if n is not None:\n        cfg.data.train_dataset_path = f\"../data_n{n}/train_raw.txt\"\n        cfg.data.test_dataset_path = f\"../data_n{n}/test_raw.txt\"\n        cfg.train.save_dir = f\"../outputs/results_n{n}\"\n        if hasattr(cfg.train, \"wandb\") and hasattr(cfg.train.wandb, \"name\"):\n            cfg.train.wandb.name = f\"scaling_n{n}\"\n\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n","parity/experiments/toy/configs/data.yaml":"# Parity task (toy): random permutation σ of {0..n-1} -> sign(σ) ∈ {+1, -1}\n\nproblem_generator:\n  n: 5            # permutation length (5! = 120 distinct inputs)\n\ndataset:\n  save_dir: \"../data\"\n  num_train_samples: 5000\n  num_test_samples: 500\n  batch_size: 1000\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sympy\"\n  save_text: true\n  save_json: false\n","parity/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", 0, 20]          # permutation elements: 0..19 (covers n ≤ 20)\n    signed: [\"\", -1, -1]          # the target token \"-1\"\n  misc: [\"+1\"]                    # the target token \"+1\"\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 0\n  allow_float: false\n\nstrict: false\ninclude_base_vocab: true\n","parity/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 64\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: parity\n    name: toy_n5\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","parity/experiments/toy/configs/train_encoder_only.yaml":"model:\n  model_type: generic\n  architecture: encoder_only      # encoder-only classification head (single answer token)\n  encoder_pooling: first          # how encoder states are pooled: mean | first ([CLS]-style)\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2           # unused in encoder_only mode\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 64\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results_encoder_only\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: parity\n    name: toy_encoder_only\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  encoder_only: true              # collator emits single-token labels\n  validate_train_tokens: false\n  validate_test_tokens: false\n","parity/experiments/toy/scripts/evaluate.py":"\"\"\"Print evaluation results for the parity toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n\n    rate = sum(g == r for g, r in zip(generated, references)) / len(references)\n    print(f\"Success rate: {100 * rate:.1f}%  ({sum(g == r for g, r in zip(generated, references))}/{len(references)})\")\n\n    even_total = sum(1 for r in references if r.strip() == \"+1\")\n    odd_total = len(references) - even_total\n    print(f\"  Even (+1): {even_total}  |  Odd (−1): {odd_total}\")\n\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    show_examples(generated, references, n=5, successes=False)\n","parity/experiments/toy/scripts/generate.py":"\"\"\"Generate the parity dataset for the toy experiment.\n\nReference: class-based instance_generator pattern from\nissac2026_experiments/digit_product/generate_dataset.py.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\n\nfrom parity.core.generator import ParityGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to data config YAML (defaults to ../configs/data.yaml).\",\n)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True)\n    problem_generator = ParityGenerator(**gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(\n        cfg.dataset,\n        instance_generator=problem_generator,\n    )\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","parity/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full parity/toy experiment end-to-end.\n\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\n\necho \"=== Step 1/3: Generate data ===\"\npython generate.py\n\necho \"=== Step 2/3: Train ===\"\npython train.py \"$@\"\n\necho \"=== Step 3/3: Evaluate ===\"\npython evaluate.py\n","parity/experiments/toy/scripts/train.py":"\"\"\"Train the parity model for the toy experiment.\n\nReference: issac2026_experiments/digit_product/train.py.\n\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom parity.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\n    \"--config_path\",\n    type=click.Path(exists=True),\n    default=None,\n    help=\"Path to train config YAML (defaults to ../configs/train.yaml).\",\n)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(config_path: str | None, dryrun: bool) -> None:\n    \"\"\"Train a Transformer to predict the sign of a permutation.\"\"\"\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"},"polynomial_addition":{"polynomial_addition/README.md":"# polynomial_addition\n\n**Task**: Running sums of random multivariate polynomials (uses SageMath to generate).\n\n*Adapted from the official [calt-codebase](https://github.com/HiroshiKERA/calt-codebase) examples.*\n\n## Data format\n\n```\nInput  : x + 2*y | y^2 | x*z\nOutput : x + 2*y | x + 2*y + y^2 | x + 2*y + y^2 + x*z\n```\n\n> **Needs SageMath** (used only when generating the dataset).\n\n## Quick start\n\n```bash\ncd polynomial_addition/experiments/toy/scripts\npython generate.py    # 1. build the dataset  -> ../data/\npython train.py       # 2. train the model    -> ../outputs/results/\npython evaluate.py    # 3. report the success rate\n```\n\nOr `bash run.sh` (add `--dryrun` for a quick smoke test).\n","polynomial_addition/core/__init__.py":"from .generator import PolynomialAdditionGenerator\nfrom .formatter import format_input, format_target\nfrom .metrics import success_rate\nfrom .train import run_training\n\n__all__ = [\"PolynomialAdditionGenerator\", \"format_input\", \"format_target\", \"success_rate\", \"run_training\"]\n","polynomial_addition/core/formatter.py":"\"\"\"String format for the polynomial_addition task.\n\nInput  : x + 2*y | y^2 | x*z\nOutput : x + 2*y | x + 2*y + y^2 | x + 2*y + y^2 + x*z\n\nThe generator already returns ready-to-write strings; these helpers mirror that.\n\"\"\"\n\n\ndef format_input(problem) -> str:\n    return str(problem)\n\n\ndef format_target(answer) -> str:\n    return str(answer)\n","polynomial_addition/core/generator.py":"\"\"\"Generate (problem, answer) pairs for the polynomial running-sum task.\n\nAdapted from HiroshiKERA/calt-codebase examples/polynomial_addition.\nInput  : several random polynomials in x, y, z, joined by \" | \"\nOutput : their running (cumulative) sums, joined by \" | \"\nSageMath is used at generation time only; the dataset itself is plain text.\n\"\"\"\n\nimport random\n\n\nclass PolynomialAdditionGenerator:\n    def __init__(self, field: str = \"ZZ\", min_len: int = 3, max_len: int = 6,\n                 max_num_terms: int = 3, max_degree: int = 3, min_degree: int = 1):\n        self.field = field\n        self.min_len = min_len\n        self.max_len = max_len\n        self.max_num_terms = max_num_terms\n        self.max_degree = max_degree\n        self.min_degree = min_degree\n\n    def __call__(self, seed: int) -> tuple[str, str]:\n        import sage.misc.randstate as randstate\n        from calt.dataset.sagemath.utils.polynomial_sampler import PolynomialSampler\n\n        random.seed(seed)\n        randstate.set_random_seed(seed)\n        length = random.randint(self.min_len, self.max_len)\n        sampler = PolynomialSampler(\n            symbols=\"x, y, z\",\n            field_str=self.field,\n            order=\"grevlex\",\n            max_num_terms=self.max_num_terms,\n            max_degree=self.max_degree,\n            min_degree=self.min_degree,\n        )\n        polys = sampler.sample(length)\n        cumsum = [sum(polys[:i]) for i in range(1, len(polys) + 1)]\n        inp = \" | \".join(str(p) for p in polys)\n        out = \" | \".join(str(p) for p in cumsum)\n        return inp, out\n","polynomial_addition/core/metrics.py":"\"\"\"Metrics for the polynomial_addition task.\"\"\"\n\n\ndef success_rate(predictions: list[str], targets: list[str]) -> float:\n    \"\"\"Fraction of predictions that exactly match the target (after .strip()).\"\"\"\n    if not targets:\n        return 0.0\n    correct = sum(1 for p, t in zip(predictions, targets) if p.strip() == t.strip())\n    return correct / len(targets)\n","polynomial_addition/core/parser.py":"\"\"\"Load-time preprocessor for the polynomial_addition task.\n\nPlain text (\"input # output\" per line) is read directly by CALT's default text\nloader — no custom preprocessor is needed.\n\"\"\"\n","polynomial_addition/core/train.py":"\"\"\"Training runner for the polynomial_addition task.\n\nPlain-text storage means CALT's default text loader handles parsing, so no\ncustom load_preprocessor is needed (same simple pipeline as the parity task).\n\"\"\"\n\nimport os\n\nfrom omegaconf import DictConfig, OmegaConf\n\nfrom shared.calt_adapter import (\n    IOPipeline,\n    ModelPipeline,\n    TrainerPipeline,\n    apply_dryrun_settings,\n)\n\n\ndef run_training(cfg: DictConfig, dryrun: bool = False) -> float:\n    \"\"\"Run the full pipeline (load -> build -> train -> evaluate).\"\"\"\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    io_dict = IOPipeline.from_config(cfg.data).build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","polynomial_addition/experiments/toy/configs/data.yaml":"# polynomial_addition task (toy) — adapted from HiroshiKERA/calt-codebase examples/polynomial_addition\n\nproblem_generator:\n  field: ZZ\n  min_len: 3\n  max_len: 6\n\ndataset:\n  save_dir: \"../data\"\n  num_train_samples: 2000\n  num_test_samples: 300\n  batch_size: 1000\n  n_jobs: 4\n  root_seed: 42\n  verbose: true\n  backend: \"sympy\"\n  save_text: true\n  save_json: false\n","polynomial_addition/experiments/toy/configs/lexer.yaml":"vocab:\n  range:\n    numbers: [\"\", -99, 99]\n  misc: [\"+\", \"-\", \"*\", \"^\", \"|\", \"x\", \"y\", \"z\"]\n  special_tokens: {}\n  flags:\n    include_base_vocab: true\n    include_base_special_tokens: true\n\nnumber_policy:\n  attach_sign: true\n  digit_group: 1\n  allow_float: false\n\nstrict: false\ninclude_base_vocab: true\n","polynomial_addition/experiments/toy/configs/train.yaml":"model:\n  model_type: generic\n  num_encoder_layers: 2\n  num_encoder_heads: 4\n  num_decoder_layers: 2\n  num_decoder_heads: 4\n  d_model: 128\n  encoder_ffn_dim: 512\n  decoder_ffn_dim: 512\n  max_sequence_length: 256\n  use_positional_embedding: learned\n\ntrain:\n  save_dir: ../outputs/results\n  num_train_epochs: 100\n  learning_rate: 0.0005\n  weight_decay: 0.01\n  warmup_ratio: 0.1\n  batch_size: 64\n  test_batch_size: 64\n  lr_scheduler_type: cosine\n  max_grad_norm: 1.0\n  optimizer: adamw_torch\n  num_workers: 2\n  seed: 42\n  wandb:\n    project: calt-experiments\n    group: polynomial_addition\n    name: toy\n    no_wandb: false\n\ndata:\n  train_dataset_path: ../data/train_raw.txt\n  test_dataset_path: ../data/test_raw.txt\n  lexer_config: ../configs/lexer.yaml\n  validate_train_tokens: false\n  validate_test_tokens: false\n","polynomial_addition/experiments/toy/scripts/evaluate.py":"\"\"\"Print evaluation results for the polynomial_addition toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared.paths import output_dir\nfrom shared.plotting import load_eval_results, show_examples\n\nif __name__ == \"__main__\":\n    results = output_dir(__file__) / \"results\"\n    generated, references = load_eval_results(results)\n    correct = sum(g == r for g, r in zip(generated, references))\n    rate = correct / len(references) if references else 0.0\n    print(f\"Success rate: {100 * rate:.1f}%  ({correct}/{len(references)})\")\n    print()\n    show_examples(generated, references, n=5, successes=True)\n    print()\n    show_examples(generated, references, n=5, successes=False)\n","polynomial_addition/experiments/toy/scripts/generate.py":"\"\"\"Generate the polynomial_addition dataset for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom calt.dataset import DatasetPipeline\n\nfrom polynomial_addition.core.generator import PolynomialAdditionGenerator\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\ndef main(config_path: str | None) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"data.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.dataset.root_seed)\n\n    gen_cfg = OmegaConf.to_container(cfg.problem_generator, resolve=True) or {}\n    problem_generator = PolynomialAdditionGenerator(**gen_cfg)\n\n    pipeline = DatasetPipeline.from_config(cfg.dataset, instance_generator=problem_generator)\n    pipeline.run()\n    print(\"Dataset generation completed\")\n\n\nif __name__ == \"__main__\":\n    main()\n","polynomial_addition/experiments/toy/scripts/run.sh":"#!/bin/bash\n# Run the full polynomial_addition/toy experiment end-to-end.\nset -e\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\ncd \"$SCRIPT_DIR\"\necho \"=== Step 1/3: Generate data ===\"\npython generate.py\necho \"=== Step 2/3: Train ===\"\npython train.py \"$@\"\necho \"=== Step 3/3: Evaluate ===\"\npython evaluate.py\n","polynomial_addition/experiments/toy/scripts/train.py":"\"\"\"Train the polynomial_addition model for the toy experiment.\"\"\"\n\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nimport click\nfrom omegaconf import OmegaConf\n\nfrom polynomial_addition.core.train import run_training\nfrom shared.paths import config_dir\nfrom shared.seed import set_seed\n\n\n@click.command()\n@click.option(\"--config_path\", type=click.Path(exists=True), default=None)\n@click.option(\"--dryrun\", is_flag=True, help=\"Reduced settings for a fast smoke test.\")\ndef main(config_path: str | None, dryrun: bool) -> None:\n    cfg_path = Path(config_path) if config_path else config_dir(__file__) / \"train.yaml\"\n    cfg = OmegaConf.load(cfg_path)\n    set_seed(cfg.train.seed)\n    success = run_training(cfg, dryrun=dryrun)\n    print(f\"Success rate: {100 * success:.1f}%\")\n\n\nif __name__ == \"__main__\":\n    main()\n"}};

export const COMMON_FILES: Record<string, string> = {"shared/README.md":"# shared/\n\nUtilities shared across all tasks. Import from here instead of duplicating code.\n\n| File | What it provides |\n|---|---|\n| `seed.py` | `set_seed(n)` — fix all random seeds (Python, NumPy, PyTorch, HuggingFace) |\n| `config.py` | `load_config(path, overrides)` — load YAML with optional CLI overrides |\n| `paths.py` | `data_dir(__file__)`, `config_dir(__file__)` — paths relative to the script |\n| `calt_adapter.py` | Re-exports `DatasetPipeline`, `IOPipeline`, `ModelPipeline`, `TrainerPipeline` |\n| `logging.py` | `CustomLoggingCallback` — logs gradient norms and GPU memory to wandb |\n| `plotting.py` | `plot_success_rate()`, `show_examples()` — for analysis notebooks |\n\n## Usage example\n\n```python\nimport sys\nfrom pathlib import Path\nsys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\nfrom shared import set_seed, load_config\nfrom shared.paths import data_dir, config_dir\n\nset_seed(42)\ncfg = load_config(config_dir(__file__) / \"train.yaml\")\n```\n","shared/__init__.py":"from .seed import set_seed\nfrom .config import load_config\nfrom .paths import experiment_dir, data_dir, output_dir, config_dir\nfrom .plotting import showcase, load_eval_results, plot_success_rate\n\n__all__ = [\n    \"set_seed\",\n    \"load_config\",\n    \"experiment_dir\",\n    \"data_dir\",\n    \"output_dir\",\n    \"config_dir\",\n    \"showcase\",\n    \"load_eval_results\",\n    \"plot_success_rate\",\n]\n","shared/calt_adapter.py":"\"\"\"\nThin wrapper around the four CALT pipelines.\n\nThis module re-exports the CALT API in a single place so that task scripts\nonly need one import instead of four, and so that future CALT API changes\nonly require updating this file.\n\nThe four CALT pipelines:\n    DatasetPipeline  →  generate (problem, answer) pairs and write to disk\n    IOPipeline       →  load data, apply preprocessors, tokenize\n    ModelPipeline    →  build the Transformer\n    TrainerPipeline  →  train + evaluate\n\nTypical usage in a task's core/train.py\n----------------------------------------\n    from shared.calt_adapter import (\n        DatasetPipeline,\n        IOPipeline,\n        ModelPipeline,\n        TrainerPipeline,\n        apply_dryrun_settings,\n        detect_lexer_format,\n        ExpandedFormLoadPreprocessor,\n        ChainLoadPreprocessor,\n    )\n\"\"\"\n\nimport yaml\nfrom pathlib import Path\n\nfrom calt.dataset import DatasetPipeline\nfrom calt.io import (\n    ChainLoadPreprocessor,\n    ExpandedFormLoadPreprocessor,\n    IOPipeline,\n    TextToSageLoadPreprocessor,\n)\nfrom calt.models import ModelPipeline\nfrom calt.trainer import TrainerPipeline, apply_dryrun_settings\n\n# ---------------------------------------------------------------------------\n# Offline-preprocessing helpers.\n#\n# These were upstreamed into the CALT library: the module moved from\n# `calt.preprocess` to `calt.io.preprocess`, and two generic helpers\n# (`build_io_pipeline_with_cache`, `build_ring_from_sampler`) were added so that\n# task runners no longer duplicate the cache cascade or the source-ring\n# reconstruction.\n#\n# We import them from the library when present and provide a local fallback so\n# this repo also runs against an older calt-x that predates the upstream move.\n# Once everyone is on the new calt-x, the fallback branches become dead code and\n# can be deleted.\n# ---------------------------------------------------------------------------\ntry:  # new calt-x: module lives under calt.io\n    from calt.io.preprocess import (\n        maybe_use_pretokenized_cache,\n        maybe_use_processed_cache,\n        preprocess_to_ids,\n        run_preprocess,\n    )\nexcept ImportError:  # old calt-x: module at top level\n    from calt.preprocess import (\n        maybe_use_pretokenized_cache,\n        maybe_use_processed_cache,\n        preprocess_to_ids,\n        run_preprocess,\n    )\n\ntry:  # new calt-x exposes the generic helpers from calt.io\n    from calt.io import build_io_pipeline_with_cache, build_ring_from_sampler\nexcept ImportError:\n    build_io_pipeline_with_cache = None\n    build_ring_from_sampler = None\n\nif build_ring_from_sampler is None:\n\n    def build_ring_from_sampler(sampler_cfg):\n        \"\"\"Fallback (old calt-x): reconstruct the source PolynomialRing.\"\"\"\n        from omegaconf import OmegaConf\n        from sage.all import GF, QQ, RR, ZZ, PolynomialRing\n\n        if isinstance(sampler_cfg, dict):\n            sampler = dict(sampler_cfg)\n        else:\n            sampler = dict(OmegaConf.to_container(sampler_cfg, resolve=True))\n        symbols = sampler.get(\"symbols\", \"x,y\")\n        field_str = sampler.get(\"field_str\", \"QQ\")\n        order = sampler.get(\"order\", \"degrevlex\")\n\n        if field_str == \"QQ\":\n            field = QQ\n        elif field_str == \"RR\":\n            field = RR\n        elif field_str == \"ZZ\":\n            field = ZZ\n        elif field_str.startswith(\"GF\"):\n            digits = field_str[2:]\n            if not digits.isdigit():\n                raise ValueError(f\"Unsupported field_str for GF: {field_str!r}\")\n            field = GF(int(digits))\n        else:\n            raise ValueError(f\"Unsupported field_str: {field_str!r}\")\n        names = [s.strip() for s in symbols.split(\",\")]\n        return PolynomialRing(field, names, order=order)\n\n\nif build_io_pipeline_with_cache is None:\n\n    def build_io_pipeline_with_cache(\n        cfg, data_cfg, training_order, lexer_format, load_preprocessor, extra_hash_bytes=b\"\"\n    ):\n        \"\"\"Fallback (old calt-x): pretok → strings → raw cache cascade.\"\"\"\n        if maybe_use_pretokenized_cache(\n            cfg, data_cfg, training_order, lexer_format, extra_hash_bytes=extra_hash_bytes\n        ):\n            return IOPipeline.from_config(cfg.data), \"pretokenized\"\n        if maybe_use_processed_cache(\n            cfg, data_cfg, training_order, lexer_format, extra_hash_bytes=extra_hash_bytes\n        ):\n            return IOPipeline.from_config(cfg.data), \"strings\"\n        io_pipeline = IOPipeline.from_config(cfg.data)\n        if load_preprocessor is not None:\n            io_pipeline.dataset_load_preprocessor = load_preprocessor\n        return io_pipeline, \"none\"\n\n\n__all__ = [\n    \"DatasetPipeline\",\n    \"IOPipeline\",\n    \"ModelPipeline\",\n    \"TrainerPipeline\",\n    \"apply_dryrun_settings\",\n    # Load preprocessors\n    \"ChainLoadPreprocessor\",\n    \"ExpandedFormLoadPreprocessor\",\n    \"TextToSageLoadPreprocessor\",\n    # Helpers\n    \"detect_lexer_format\",\n    # Offline preprocessing (re-exported / shimmed from calt)\n    \"build_io_pipeline_with_cache\",\n    \"build_ring_from_sampler\",\n    \"maybe_use_pretokenized_cache\",\n    \"maybe_use_processed_cache\",\n    \"preprocess_to_ids\",\n    \"run_preprocess\",\n]\n\n\ndef detect_lexer_format(lexer_yaml_path: str | Path) -> str:\n    \"\"\"\n    Detect whether a lexer.yaml uses the 'raw' or 'expanded' polynomial format.\n\n    Returns\n    -------\n    \"expanded\"  if vocab.range has BOTH `coefficients` and `exponents` keys\n                 (e.g. `coefficients: [\"C\", -99, 99]`, `exponents: [\"E\", 0, 5]`)\n    \"raw\"       otherwise (default: vocab.range has just `numbers`)\n\n    Background\n    ----------\n    Polynomials can be tokenized in two ways (see paper §2.2):\n      - raw      : text-direct, e.g. \"x ^ 2 + y\" (used by ISSAC2026/groebner)\n      - expanded : C/E form,   e.g. \"C1 E2 E0 + C1 E0 E1\" (used by ISSAC2026/polynomial_reduction)\n\n    The vocabulary required differs between the two formats. By inspecting the\n    `vocab.range` keys, we can detect which format the user wants:\n\n        vocab.range.numbers      → raw text format\n        vocab.range.coefficients → C/E expanded format\n        vocab.range.exponents    → C/E expanded format\n\n    \"\"\"\n    lexer_yaml_path = Path(lexer_yaml_path)\n    if not lexer_yaml_path.exists():\n        # Try to suggest available lexer files in the same directory.\n        parent = lexer_yaml_path.parent if lexer_yaml_path.parent.exists() else Path(\".\")\n        candidates = sorted(parent.glob(\"lexer*.yaml\"))\n        suggestion = \"\"\n        if candidates:\n            suggestion = (\n                \"\\n  Available lexer configs in that directory:\\n    \"\n                + \"\\n    \".join(f\"- {c.name}\" for c in candidates)\n            )\n        raise FileNotFoundError(\n            f\"Lexer config not found: {lexer_yaml_path}\\n\"\n            f\"  Check `data.lexer_config` in your train.yaml.{suggestion}\"\n        )\n\n    with open(lexer_yaml_path) as f:\n        cfg = yaml.safe_load(f) or {}\n    rng = (cfg.get(\"vocab\") or {}).get(\"range\") or {}\n    if \"coefficients\" in rng and \"exponents\" in rng:\n        return \"expanded\"\n    return \"raw\"\n\n\ndef maybe_wrap_with_expanded_preprocessor(\n    io_pipeline,\n    lexer_yaml_path: str | Path,\n    *,\n    delimiter: str = \"|\",\n    ring=None,\n):\n    \"\"\"\n    If the lexer is in C/E expanded format, wire `ExpandedFormLoadPreprocessor`\n    into the io_pipeline's load chain. Otherwise leave the pipeline unchanged.\n\n    The chain becomes:\n        [existing dataset_load_preprocessor if any]\n        → TextToSageLoadPreprocessor (raw text → Sage polynomials)\n        → ExpandedFormLoadPreprocessor (Sage polys → \"C1 E2 E0\" text)\n\n    Parameters\n    ----------\n    io_pipeline : IOPipeline\n        Mutated in place if expanded format is detected.\n    lexer_yaml_path : str | Path\n        Path to the lexer.yaml whose format determines the behavior.\n    delimiter : str\n        Inner separator between polynomials (default \"|\").\n    ring : sage PolynomialRing or None\n        Required for TextToSageLoadPreprocessor when format is expanded.\n        Must be provided by the caller (different rings per task).\n\n    Returns\n    -------\n    str : the detected format (\"raw\" or \"expanded\"), for logging.\n    \"\"\"\n    fmt = detect_lexer_format(lexer_yaml_path)\n    if fmt != \"expanded\":\n        return fmt\n    if ring is None:\n        raise ValueError(\n            \"Expanded format requires a `ring` argument to TextToSageLoadPreprocessor. \"\n            \"Pass the SageMath PolynomialRing matching your data.\"\n        )\n    text_to_sage = TextToSageLoadPreprocessor(delimiter=delimiter, ring=ring)\n    expanded_form = ExpandedFormLoadPreprocessor(delimiter=delimiter)\n    existing = io_pipeline.dataset_load_preprocessor\n    if existing is None:\n        io_pipeline.dataset_load_preprocessor = ChainLoadPreprocessor(text_to_sage, expanded_form)\n    else:\n        # Insert expanded_form at the end of the existing chain\n        io_pipeline.dataset_load_preprocessor = ChainLoadPreprocessor(existing, expanded_form)\n    return fmt\n\n\ndef run_standard_training(cfg, load_preprocessor=None, dryrun: bool = False) -> float:\n    \"\"\"\n    Run a complete training pipeline: load data → build model → train → evaluate.\n\n    Parameters\n    ----------\n    cfg : DictConfig\n    load_preprocessor : object | None\n        Optional load-time preprocessor (must implement process_sample).\n    dryrun : bool\n\n    Returns\n    -------\n    float : exact-match success rate on the test set.\n    \"\"\"\n    import os\n    from omegaconf import OmegaConf\n\n    if dryrun:\n        apply_dryrun_settings(cfg)\n\n    save_dir = cfg.train.get(\"save_dir\", cfg.train.get(\"output_dir\", \"./results\"))\n    os.makedirs(save_dir, exist_ok=True)\n    OmegaConf.save(cfg, os.path.join(save_dir, \"train.yaml\"))\n\n    io_pipeline = IOPipeline.from_config(cfg.data)\n    if load_preprocessor is not None:\n        io_pipeline.dataset_load_preprocessor = load_preprocessor\n\n    io_dict = io_pipeline.build()\n    model = ModelPipeline.from_io_dict(cfg.model, io_dict).build()\n    trainer_pipeline = TrainerPipeline.from_io_dict(cfg.train, model, io_dict).build()\n\n    trainer_pipeline.train()\n    trainer_pipeline.save_model()\n    return trainer_pipeline.evaluate_and_save_generation()\n","shared/config.py":"\"\"\"OmegaConf helpers: load YAML configs with optional command-line overrides.\"\"\"\n\nfrom pathlib import Path\nfrom typing import Union\n\nfrom omegaconf import DictConfig, OmegaConf\n\n\ndef load_config(path: Union[str, Path], overrides: list[str] | None = None) -> DictConfig:\n    \"\"\"\n    Load a YAML config file and optionally apply dot-notation overrides.\n\n    Parameters\n    ----------\n    path : str | Path\n        Path to the YAML config file.\n    overrides : list[str] | None\n        List of OmegaConf overrides in \"key=value\" format.\n        Example: [\"train.learning_rate=0.001\", \"train.num_train_epochs=50\"]\n\n    Returns\n    -------\n    DictConfig\n        The loaded (and optionally overridden) config.\n\n    Example\n    -------\n    >>> cfg = load_config(\"configs/train.yaml\", [\"train.batch_size=32\"])\n    >>> print(cfg.train.learning_rate)\n    \"\"\"\n    cfg = OmegaConf.load(path)\n    if overrides:\n        override_cfg = OmegaConf.from_dotlist(overrides)\n        cfg = OmegaConf.merge(cfg, override_cfg)\n    return cfg\n\n\ndef save_config(cfg: DictConfig, path: Union[str, Path]) -> None:\n    \"\"\"Save a config to a YAML file (useful for logging experiment settings).\"\"\"\n    OmegaConf.save(cfg, path)\n","shared/hooks.py":"\"\"\"\nCLI to manage user post-processing hooks (task 3).\n\nUsage from the repo root\n------------------------\n    python shared/hooks.py list                  # show active vs available\n    python shared/hooks.py enable base           # activate base hook\n    python shared/hooks.py enable groebner       # activate per-task hook\n    python shared/hooks.py disable base          # remove base hook\n    python shared/hooks.py disable groebner      # remove per-task hook\n\nEach `enable` copies the corresponding `.example` template to the live path.\nEach `disable` deletes the live file. After both operations, run\n`python <task>/.../preprocess.py --force` to rebuild caches.\n\nTemplates available (matches `get_user_postprocessors`):\n    shared/base_postprocessor.py.example          → shared/base_postprocessor.py\n    groebner_basis/core/postprocessor.py.example  → groebner_basis/core/postprocessor.py\n    border_basis/core/postprocessor.py.example    → border_basis/core/postprocessor.py\n    parity/core/postprocessor.py.example          → parity/core/postprocessor.py\n\"\"\"\n\nfrom __future__ import annotations\n\nimport shutil\nimport sys\nfrom pathlib import Path\n\n# Repo root inferred from this file location\nROOT = Path(__file__).resolve().parent.parent\n\nSLOTS = {\n    # name → (live path relative to ROOT, template path relative to ROOT)\n    \"base\": (\n        \"shared/base_postprocessor.py\",\n        \"shared/base_postprocessor.py.example\",\n    ),\n    \"groebner\": (\n        \"groebner_basis/core/postprocessor.py\",\n        \"groebner_basis/core/postprocessor.py.example\",\n    ),\n    \"border\": (\n        \"border_basis/core/postprocessor.py\",\n        \"border_basis/core/postprocessor.py.example\",\n    ),\n    \"parity\": (\n        \"parity/core/postprocessor.py\",\n        \"parity/core/postprocessor.py.example\",\n    ),\n}\n\n\ndef cmd_list() -> int:\n    \"\"\"Print a table of slot status: active / template-only / missing.\"\"\"\n    print(f\"Repo root: {ROOT}\\n\")\n    print(f\"{'Slot':<12} {'Status':<22} {'File'}\")\n    print(\"-\" * 70)\n    for name, (live_rel, tmpl_rel) in SLOTS.items():\n        live = ROOT / live_rel\n        tmpl = ROOT / tmpl_rel\n        if live.exists():\n            status = \"✓ ACTIVE\"\n            path = str(live_rel)\n        elif tmpl.exists():\n            status = \"○ template only\"\n            path = str(tmpl_rel)\n        else:\n            status = \"✗ MISSING\"\n            path = f\"(expected at {live_rel})\"\n        print(f\"{name:<12} {status:<22} {path}\")\n    print()\n    print(\"Edit a live file (✓ ACTIVE) to change the hook behavior.\")\n    print(\"After enable/disable/edit, rebuild caches: python <task>/.../preprocess.py --force\")\n    return 0\n\n\ndef cmd_enable(name: str) -> int:\n    if name not in SLOTS:\n        print(f\"Unknown slot: {name!r}. Valid: {list(SLOTS)}\", file=sys.stderr)\n        return 2\n    live_rel, tmpl_rel = SLOTS[name]\n    live = ROOT / live_rel\n    tmpl = ROOT / tmpl_rel\n    if live.exists():\n        print(f\"Slot {name!r} is already ACTIVE at {live_rel}\")\n        print(f\"  → edit it directly, then `--force` your preprocess.py to rebuild caches.\")\n        return 0\n    if not tmpl.exists():\n        print(f\"Template not found: {tmpl_rel}\", file=sys.stderr)\n        return 2\n    shutil.copyfile(tmpl, live)\n    print(f\"✓ Enabled slot {name!r}: copied {tmpl_rel} → {live_rel}\")\n    print(f\"  Now edit {live_rel} to write your transformation.\")\n    print(f\"  Then rebuild caches: python <task>/.../preprocess.py --force\")\n    return 0\n\n\ndef cmd_disable(name: str) -> int:\n    if name not in SLOTS:\n        print(f\"Unknown slot: {name!r}. Valid: {list(SLOTS)}\", file=sys.stderr)\n        return 2\n    live_rel, _ = SLOTS[name]\n    live = ROOT / live_rel\n    if not live.exists():\n        print(f\"Slot {name!r} was not active (no file at {live_rel}). Nothing to do.\")\n        return 0\n    live.unlink()\n    print(f\"✓ Disabled slot {name!r}: removed {live_rel}\")\n    print(f\"  Caches were hashed with the hook source — they will be detected STALE on next run.\")\n    print(f\"  Rebuild caches: python <task>/.../preprocess.py --force\")\n    return 0\n\n\ndef main(argv: list[str]) -> int:\n    if len(argv) < 1:\n        print(__doc__)\n        return 1\n    cmd = argv[0]\n    if cmd in (\"-h\", \"--help\", \"help\"):\n        print(__doc__)\n        return 0\n    if cmd == \"list\":\n        return cmd_list()\n    if cmd == \"enable\":\n        if len(argv) < 2:\n            print(\"Usage: enable <slot>     (slots: base, groebner, border, parity)\", file=sys.stderr)\n            return 2\n        return cmd_enable(argv[1])\n    if cmd == \"disable\":\n        if len(argv) < 2:\n            print(\"Usage: disable <slot>    (slots: base, groebner, border, parity)\", file=sys.stderr)\n            return 2\n        return cmd_disable(argv[1])\n    print(f\"Unknown command: {cmd!r}. Valid: list | enable | disable\", file=sys.stderr)\n    return 2\n\n\nif __name__ == \"__main__\":\n    sys.exit(main(sys.argv[1:]))\n","shared/inspect_cache.py":"\"\"\"\nCLI to inspect preprocessing caches.\n\nUsage from the repo root\n------------------------\n    python shared/inspect_cache.py                                  # scan all data/ dirs\n    python shared/inspect_cache.py groebner_basis/.../data/QQ       # one specific dir\n\nFor each cache directory found (`processed_*` or `processed_*_ids`), prints:\n  - kind (strings / pretokenized)\n  - training_order + lexer_format\n  - sample counts (train / test)\n  - creation time\n  - hash digest (12 first chars)\n  - tokenizer vocab size (for pretokenized only)\n\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nimport sys\nfrom datetime import datetime\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parent.parent\n\n\ndef _count_jsonl_lines(path: Path) -> int:\n    n = 0\n    if not path.exists():\n        return 0\n    with open(path, \"rb\") as f:\n        for _ in f:\n            n += 1\n    return n\n\n\ndef inspect_cache_dir(cache_dir: Path) -> None:\n    \"\"\"Print a one-cache summary.\"\"\"\n    meta_path = cache_dir / \"_meta.yaml\"\n    hash_path = cache_dir / \"_hash.txt\"\n    is_pretok = \"_ids\" in cache_dir.name\n\n    print(f\"\\n  {cache_dir.relative_to(ROOT) if cache_dir.is_relative_to(ROOT) else cache_dir}\")\n    print(f\"    kind          : {'pretokenized (input_ids)' if is_pretok else 'strings (problem/answer)'}\")\n\n    if meta_path.exists():\n        try:\n            import yaml as _yaml\n            meta = _yaml.safe_load(meta_path.read_text()) or {}\n        except Exception:\n            meta = {}\n        order = meta.get(\"training_order\", \"?\")\n        fmt = meta.get(\"lexer_format\", \"?\")\n        created = meta.get(\"created_at\", \"?\")\n        n_train = meta.get(\"n_train\", \"?\")\n        n_test = meta.get(\"n_test\", \"?\")\n        load_pp = meta.get(\"load_preprocessor\", \"?\")\n        vocab = meta.get(\"tokenizer_vocab_size\", None)\n        print(f\"    order/format  : {order} / {fmt}\")\n        print(f\"    samples       : train={n_train}, test={n_test}\")\n        print(f\"    created       : {created}\")\n        print(f\"    load chain    : {load_pp}\")\n        if vocab is not None:\n            print(f\"    vocab size    : {vocab}\")\n    else:\n        # Fallback: read JSONL line counts directly\n        train_name = \"train_ids.jsonl\" if is_pretok else \"train_processed.jsonl\"\n        test_name = \"test_ids.jsonl\" if is_pretok else \"test_processed.jsonl\"\n        n_tr = _count_jsonl_lines(cache_dir / train_name)\n        n_te = _count_jsonl_lines(cache_dir / test_name)\n        print(f\"    samples       : train={n_tr}, test={n_te} (no _meta.yaml)\")\n\n    if hash_path.exists():\n        h = hash_path.read_text().strip()\n        print(f\"    hash          : {h[:12]}...\")\n    else:\n        print(f\"    hash          : MISSING\")\n\n\ndef scan(root: Path) -> list[Path]:\n    \"\"\"Find all cache directories under root.\"\"\"\n    found = []\n    for d in sorted(root.rglob(\"processed_*\")):\n        if d.is_dir():\n            found.append(d)\n    return found\n\n\ndef main(argv: list[str]) -> int:\n    if argv and argv[0] in (\"-h\", \"--help\", \"help\"):\n        print(__doc__)\n        return 0\n\n    if argv:\n        target = Path(argv[0]).resolve()\n        if not target.exists():\n            print(f\"Directory not found: {target}\", file=sys.stderr)\n            return 2\n    else:\n        target = ROOT\n\n    caches = scan(target)\n    if not caches:\n        print(f\"No cache directories found under {target}\")\n        print(\"Hint: run `python <task>/experiments/<exp>/scripts/preprocess.py` to build one.\")\n        return 0\n\n    print(f\"Found {len(caches)} cache director{'y' if len(caches)==1 else 'ies'} under {target}:\")\n    for d in caches:\n        inspect_cache_dir(d)\n    print()\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main(sys.argv[1:]))\n","shared/logging.py":"\"\"\"Custom HuggingFace callback that logs extra metrics to Weights & Biases.\"\"\"\n\nimport torch\nimport wandb\nfrom transformers import TrainerCallback\n\n\nclass CustomLoggingCallback(TrainerCallback):\n    \"\"\"\n    Log gradient norms and GPU memory usage to wandb at each logging step.\n\n    Usage\n    -----\n    Add to TrainerPipeline after building:\n\n        from shared.logging import CustomLoggingCallback\n        trainer_pipeline.trainer.add_callback(CustomLoggingCallback())\n    \"\"\"\n\n    def on_log(self, args, state, control, model=None, **kwargs):\n        if not state.is_world_process_zero or model is None:\n            return\n\n        metrics = {}\n\n        # Average L2 norm of trainable parameters\n        with torch.no_grad():\n            param_norm_sq = sum(\n                torch.norm(p).item() ** 2\n                for p in model.parameters()\n                if p.requires_grad\n            )\n            param_count = sum(\n                p.numel() for p in model.parameters() if p.requires_grad\n            )\n            if param_count > 0:\n                metrics[\"train/avg_param_norm\"] = (param_norm_sq / param_count) ** 0.5\n\n        # GPU memory\n        if torch.cuda.is_available():\n            metrics[\"train/gpu_memory_used_MB\"] = torch.cuda.memory_allocated() / 1024**2\n            metrics[\"train/gpu_memory_reserved_MB\"] = torch.cuda.memory_reserved() / 1024**2\n\n        if metrics:\n            wandb.log(metrics)\n","shared/paths.py":"\"\"\"\nPath helpers for experiment scripts.\n\nEach experiment script lives at:\n    <task>/experiments/<exp_name>/scripts/<script>.py\n\nThe helpers below derive standard paths relative to any script file,\nso scripts work correctly regardless of which directory you cd into.\n\nExample usage in a script:\n    import sys\n    from pathlib import Path\n    sys.path.insert(0, str(Path(__file__).resolve().parents[4]))\n\n    from shared.paths import experiment_dir, data_dir, config_dir\n\n    DATA   = data_dir(__file__)    # → <task>/experiments/<exp_name>/data/\n    CONFIG = config_dir(__file__)  # → <task>/experiments/<exp_name>/configs/\n    OUT    = output_dir(__file__)  # → <task>/experiments/<exp_name>/outputs/\n\"\"\"\n\nfrom pathlib import Path\n\n\ndef experiment_dir(script_file: str) -> Path:\n    \"\"\"Return the experiment directory (parent of the scripts/ folder).\"\"\"\n    return Path(script_file).resolve().parent.parent\n\n\ndef data_dir(script_file: str) -> Path:\n    \"\"\"Return the data directory for the current experiment.\"\"\"\n    return experiment_dir(script_file) / \"data\"\n\n\ndef output_dir(script_file: str) -> Path:\n    \"\"\"Return the outputs directory for the current experiment.\"\"\"\n    return experiment_dir(script_file) / \"outputs\"\n\n\ndef config_dir(script_file: str) -> Path:\n    \"\"\"Return the configs directory for the current experiment.\"\"\"\n    return experiment_dir(script_file) / \"configs\"\n\n\ndef codebase_root(script_file: str) -> Path:\n    \"\"\"\n    Return the calt-codebase-new root (4 levels above a script in\n    <task>/experiments/<exp>/scripts/).\n    \"\"\"\n    return Path(script_file).resolve().parents[4]\n","shared/plotting.py":"\"\"\"Plotting and inspection utilities for experiment analysis notebooks.\n\nIncludes a `showcase()` helper that mirrors `calt-codebase/examples/demos/utils.py`\n(the canonical helper used by the official `minimal_demo.ipynb`).\n\nFor raw access to the eval results, `load_eval_results` and `plot_success_rate`\ndelegate to `calt.io.visualization.comparison_vis.load_eval_results` — the\ncanonical loader for the eval JSON written by `evaluate_and_save_generation`.\n\nThe on-disk format is a JSON list of dicts:\n    [{\"generated\": \"...\", \"reference\": \"...\"}, ...]\n\"\"\"\n\nimport json\nimport os\nfrom pathlib import Path\n\nimport matplotlib.pyplot as plt\n\nfrom calt.io.visualization.comparison_vis import (\n    load_eval_results as _calt_load_eval_results,\n)\n\n\n# ---------------------------------------------------------------------------\n# Eval result loading\n# ---------------------------------------------------------------------------\n\ndef _resolve_eval_results_path(\n    results_dir: str | Path,\n    eval_results_path: str | None = None,\n) -> Path:\n    \"\"\"\n    Resolve the path to an eval results JSON.\n\n    Preference order:\n      1. Explicit `eval_results_path` if given.\n      2. ``<results_dir>/eval_results.json`` if present.\n      3. The most recent ``step_*.json`` inside ``<results_dir>/eval_results/``.\n\n    Raises FileNotFoundError with a helpful message otherwise.\n    \"\"\"\n    if eval_results_path is not None:\n        return Path(eval_results_path)\n\n    results_dir = Path(results_dir)\n    direct = results_dir / \"eval_results.json\"\n    if direct.is_file():\n        return direct\n\n    step_dir = results_dir / \"eval_results\"\n    if step_dir.is_dir():\n        steps = sorted(\n            step_dir.glob(\"step_*.json\"),\n            key=lambda p: int(p.stem.replace(\"step_\", \"\")),\n        )\n        if steps:\n            return steps[-1]\n\n    raise FileNotFoundError(\n        f\"No eval_results.json or eval_results/step_*.json under {results_dir}. \"\n        \"Run train.py first.\"\n    )\n\n\ndef load_eval_results(results_dir: str | Path) -> tuple[list[str], list[str]]:\n    \"\"\"\n    Load the latest evaluation results from a results directory.\n\n    Returns (generated_texts, reference_texts).\n    \"\"\"\n    path = _resolve_eval_results_path(results_dir)\n    return _calt_load_eval_results(str(path))\n\n\n# ---------------------------------------------------------------------------\n# showcase: port of calt-codebase/examples/demos/utils.py::showcase\n# ---------------------------------------------------------------------------\n\ndef showcase(\n    dataset,\n    success_cases: bool = True,\n    num_show: int = 5,\n    eval_results_path: str | None = None,\n    results_dir: str = \"results\",\n) -> None:\n    \"\"\"\n    Print up to `num_show` success (or failure) cases from the latest eval results.\n\n    Direct port of `calt-codebase/examples/demos/utils.py::showcase` so notebook\n    output matches the official `minimal_demo.ipynb`.\n\n    Parameters\n    ----------\n    dataset\n        The test_dataset returned by `IOPipeline.build()`. Its `preprocessor`\n        is used to decode the tokenized strings stored in eval results.\n    success_cases : bool\n        True → print correct predictions. False → print mistakes.\n    num_show : int\n        Maximum number of cases to print.\n    eval_results_path : str | None\n        Explicit path to an eval JSON. If None, auto-resolved from `results_dir`.\n    results_dir : str\n        Output directory of the training run (default \"results\").\n    \"\"\"\n    if success_cases:\n        indicator_fn = lambda gen, ref: gen == ref  # noqa: E731\n        tag = \"success\"\n    else:\n        indicator_fn = lambda gen, ref: gen != ref  # noqa: E731\n        tag = \"failure\"\n\n    path = _resolve_eval_results_path(results_dir, eval_results_path)\n    gen_texts, ref_texts = _calt_load_eval_results(str(path))\n    cases = [\n        (i, gen, ref)\n        for i, (gen, ref) in enumerate(zip(gen_texts, ref_texts))\n        if indicator_fn(gen, ref)\n    ]\n\n    print(\"-------------------------\")\n    print(f\"\"\" {tag} cases ({len(cases)} total) \"\"\")\n    print(\"-------------------------\")\n    for i, gen, ref in cases[:num_show]:\n        gen_expr = dataset.preprocessor.decode(gen)\n        ref_expr = dataset.preprocessor.decode(ref)\n        print(f\"  [{i}] gen: {gen_expr}  |  ref: {ref_expr}\")\n\n\n# ---------------------------------------------------------------------------\n# show_examples: print n success or failure cases from pre-loaded lists\n# ---------------------------------------------------------------------------\n\ndef show_examples(\n    generated: list[str],\n    references: list[str],\n    n: int = 5,\n    successes: bool = True,\n) -> None:\n    \"\"\"\n    Print up to `n` success (or failure) cases from pre-loaded result lists.\n\n    Parameters\n    ----------\n    generated : list[str]\n        Model predictions (as returned by load_eval_results).\n    references : list[str]\n        Ground-truth strings.\n    n : int\n        Maximum number of cases to print.\n    successes : bool\n        True → print correct predictions. False → print mistakes.\n    \"\"\"\n    tag = \"success\" if successes else \"failure\"\n    cases = [\n        (i, gen, ref)\n        for i, (gen, ref) in enumerate(zip(generated, references))\n        if (gen == ref) == successes\n    ]\n    print(\"-------------------------\")\n    print(f\" {tag} cases ({len(cases)} total) \")\n    print(\"-------------------------\")\n    for i, gen, ref in cases[:n]:\n        print(f\"  [{i}] predicted : {gen}\")\n        print(f\"       reference : {ref}\")\n\n\n# ---------------------------------------------------------------------------\n# Plotting\n# ---------------------------------------------------------------------------\n\ndef plot_success_rate(results_dir: str | Path, ax=None) -> plt.Axes:\n    \"\"\"\n    Plot exact-match success rate over training steps.\n\n    Reads all ``step_*.json`` files in ``<results_dir>/eval_results/`` and\n    plots success rate vs step.\n    \"\"\"\n    results_dir = Path(results_dir)\n    step_dir = results_dir / \"eval_results\"\n    if not step_dir.is_dir():\n        raise FileNotFoundError(f\"No eval_results/ directory in {results_dir}\")\n\n    steps, rates = [], []\n    for p in sorted(step_dir.glob(\"step_*.json\"), key=lambda p: int(p.stem.replace(\"step_\", \"\"))):\n        step = int(p.stem.replace(\"step_\", \"\"))\n        with open(p) as f:\n            data = json.load(f)\n        if not data:\n            continue\n        gen = [item.get(\"generated\", \"\") for item in data]\n        ref = [item.get(\"reference\", \"\") for item in data]\n        rate = sum(g == r for g, r in zip(gen, ref)) / len(ref)\n        steps.append(step)\n        rates.append(rate * 100)\n\n    if ax is None:\n        _, ax = plt.subplots(figsize=(8, 4))\n    ax.plot(steps, rates, marker=\"o\", linewidth=2)\n    ax.set_xlabel(\"Training step\")\n    ax.set_ylabel(\"Success rate (%)\")\n    ax.set_title(\"Exact-match success rate\")\n    ax.grid(True, alpha=0.3)\n    return ax\n","shared/preprocess.py":"\"\"\"\nRe-export shim for `calt.preprocess`.\n\nThe canonical implementation now lives in the CALT library\n(`calt/io/preprocess.py`, formerly `calt/preprocess.py`) so that any project\nbuilding on CALT can benefit from offline preprocessing without re-implementing\nit.\n\nThis file is kept only for backwards compatibility with the small number of\nimports that already write `from shared.preprocess import ...`. New code should\nimport from `calt.io.preprocess` directly (or from `shared.calt_adapter`, which\nshims both the new and old module locations).\n\nIf you ever `pip install --upgrade calt-x` and the `calt/preprocess.py` file\ndisappears from site-packages, restore it from this project's backup:\n\n    /data/t-maxime/backups/calt-sitepackages_before-preprocess_20260526.tar.gz\n\nor copy the previous version of this file (see git history) which contained\nthe full implementation inline.\n\"\"\"\n\ntry:  # new calt-x: module relocated under calt.io\n    from calt.io.preprocess import (  # noqa: F401\n        PREPROCESS_VERSION,\n        compute_config_hash,\n        get_cache_dir,\n        maybe_use_processed_cache,\n        preprocess_to_ids,\n        run_preprocess,\n    )\nexcept ImportError:  # old calt-x: module at top level\n    from calt.preprocess import (  # noqa: F401\n        PREPROCESS_VERSION,\n        compute_config_hash,\n        get_cache_dir,\n        maybe_use_processed_cache,\n        preprocess_to_ids,\n        run_preprocess,\n    )\n\n__all__ = [\n    \"PREPROCESS_VERSION\",\n    \"compute_config_hash\",\n    \"get_cache_dir\",\n    \"maybe_use_processed_cache\",\n    \"preprocess_to_ids\",\n    \"run_preprocess\",\n]\n","shared/seed.py":"\"\"\"Fix all random seeds for fully reproducible experiments.\"\"\"\n\nimport os\nimport random\n\nimport numpy as np\nimport torch\nfrom transformers import set_seed as hf_set_seed\n\n\ndef set_seed(seed: int = 42) -> None:\n    \"\"\"\n    Fix random seeds for Python, NumPy, PyTorch, and HuggingFace.\n\n    Call this at the top of any training or generation script.\n\n    Parameters\n    ----------\n    seed : int\n        The seed value. Default is 42.\n    \"\"\"\n    os.environ[\"PYTHONHASHSEED\"] = str(seed)\n    random.seed(seed)\n    np.random.seed(seed)\n    torch.manual_seed(seed)\n    torch.cuda.manual_seed(seed)\n    torch.cuda.manual_seed_all(seed)\n    torch.backends.cudnn.benchmark = False\n    torch.backends.cudnn.deterministic = True\n    hf_set_seed(seed)\n    # Required env var for deterministic CuBLAS on CUDA >= 10.2\n    os.environ.setdefault(\"CUBLAS_WORKSPACE_CONFIG\", \":4096:8\")\n    torch.use_deterministic_algorithms(True)\n","shared/user_postprocessor.py":"\"\"\"\nUser-defined post-processing hooks (task 3).\n\nTwo optional slots, applied AFTER each task's load-time chain has produced\n`(input_text, target_text)`. Both are pure-Python functions the user can write.\n\n    1. Base hook   — file `shared/base_postprocessor.py`\n                     Applied to ALL tasks that wire postprocessing in their\n                     `build_load_preprocessor`.\n\n    2. Per-task hook — file `<task>/core/postprocessor.py`\n                       Applied only to that specific task, AFTER the base hook.\n\nEach hook must expose a function with the exact signature:\n\n    def postprocess(input_text: str, target_text: str) -> tuple[str, str]:\n        return input_text, target_text   # default: identity\n\nBoth files are OPT-IN: if the file does not exist, the corresponding hook is\nskipped and the chain stays exactly as it was before task 3 — no behavior\nchange, no cache invalidation.\n\nHash sensitivity\n----------------\nWhen a hook is wired, its source code is included in the preprocessing cache\nhash, so any edit to a hook function invalidates the cache (preventing stale\ndata from being reused with mismatched preprocessing).\n\nUsage from a task's `build_load_preprocessor`\n---------------------------------------------\n    from shared.user_postprocessor import (\n        get_user_postprocessors,\n        UserPostProcessorAdapter,\n        RawLineToTupleAdapter,\n    )\n\n    funcs = get_user_postprocessors(task_pkg=\"groebner_basis\")\n    if funcs:\n        # If task's own chain is empty, ensure we parse raw line first\n        if not chain:\n            chain.append(RawLineToTupleAdapter())\n        chain.append(UserPostProcessorAdapter(funcs))\n\"\"\"\n\nfrom __future__ import annotations\n\nimport hashlib\nimport importlib\nimport inspect\nimport linecache\nfrom pathlib import Path\nfrom typing import Callable\n\nPostFn = Callable[[str, str], tuple[str, str]]\n\n\n# --------------------------------------------------------------------------- #\n# Hook loading                                                                 #\n# --------------------------------------------------------------------------- #\n\ndef _try_import_postprocess(module_name: str) -> PostFn | None:\n    \"\"\"Import `<module_name>.postprocess` if it exists; return None otherwise.\n\n    Raises a clear error if the file exists but the function is malformed\n    (wrong name, wrong signature, wrong return type) instead of silently\n    skipping the hook.\n    \"\"\"\n    try:\n        mod = importlib.import_module(module_name)\n    except ModuleNotFoundError:\n        return None\n    fn = getattr(mod, \"postprocess\", None)\n    if fn is None or not callable(fn):\n        raise AttributeError(\n            f\"Hook file `{module_name}` exists but does not expose a callable \"\n            f\"named `postprocess`.\\n\"\n            f\"  Required signature:\\n\"\n            f\"      def postprocess(input_text: str, target_text: str) -> tuple[str, str]:\\n\"\n            f\"          return input_text, target_text\\n\"\n            f\"  Did you typo the function name? It MUST be exactly `postprocess`.\"\n        )\n\n    try:\n        sig = inspect.signature(fn)\n    except (TypeError, ValueError):\n        return fn  # builtin/unanalyzable; trust the user.\n    params = [\n        p for p in sig.parameters.values()\n        if p.kind in (inspect.Parameter.POSITIONAL_ONLY, inspect.Parameter.POSITIONAL_OR_KEYWORD)\n    ]\n    if len(params) != 2:\n        raise TypeError(\n            f\"Hook `{module_name}.postprocess` must take exactly 2 positional \"\n            f\"arguments `(input_text, target_text)`, got {len(params)}: \"\n            f\"{[p.name for p in params]}.\\n\"\n            f\"  Required signature:\\n\"\n            f\"      def postprocess(input_text: str, target_text: str) -> tuple[str, str]:\"\n        )\n\n    # Dummy call: catches runtime errors + validates return type early.\n    try:\n        result = fn(\"dummy_input\", \"dummy_target\")\n    except Exception as e:\n        raise RuntimeError(\n            f\"Hook `{module_name}.postprocess` crashed on dummy input \"\n            f\"('dummy_input', 'dummy_target'):\\n\"\n            f\"  {type(e).__name__}: {e}\\n\"\n            f\"  Make sure your function handles any (str, str) input without crashing.\"\n        ) from e\n    if not (\n        isinstance(result, tuple)\n        and len(result) == 2\n        and isinstance(result[0], str)\n        and isinstance(result[1], str)\n    ):\n        raise TypeError(\n            f\"Hook `{module_name}.postprocess` must return `tuple[str, str]`, \"\n            f\"got {type(result).__name__} = {result!r}.\\n\"\n            f\"  Fix the return statement:\\n\"\n            f\"      return input_text, target_text   # both strings, in a 2-tuple\"\n        )\n    return fn\n\n\ndef get_user_postprocessors(task_pkg: str) -> list[PostFn]:\n    \"\"\"\n    Return the list of postprocess hooks to apply, in execution order.\n\n    Order: [base, task_specific]. Empty list if neither file exists.\n\n    Parameters\n    ----------\n    task_pkg : str\n        The Python package name of the task (e.g. \"groebner_basis\").\n        Looks for `<task_pkg>.core.postprocessor.postprocess`.\n    \"\"\"\n    hooks: list[PostFn] = []\n    base = _try_import_postprocess(\"shared.base_postprocessor\")\n    if base is not None:\n        hooks.append(base)\n    task = _try_import_postprocess(f\"{task_pkg}.core.postprocessor\")\n    if task is not None:\n        hooks.append(task)\n    return hooks\n\n\n# --------------------------------------------------------------------------- #\n# Cache-hash contribution                                                      #\n# --------------------------------------------------------------------------- #\n\ndef user_postprocessor_hash_contribution(task_pkg: str) -> bytes:\n    \"\"\"\n    Return the bytes that should be folded into the preprocessing cache hash.\n\n    For each hook present, includes the FULL source of its module. This way\n    edits to a hook function invalidate the cache; absent hooks contribute\n    empty bytes (no behavior change for users who never write a hook).\n    \"\"\"\n    h = hashlib.sha256()\n    for module_name in (\"shared.base_postprocessor\", f\"{task_pkg}.core.postprocessor\"):\n        try:\n            mod = importlib.import_module(module_name)\n            # Invalidate linecache for this file so inspect.getsource picks up\n            # post-edit content rather than returning a stale cached source.\n            src_file = inspect.getsourcefile(mod)\n            if src_file:\n                linecache.checkcache(src_file)\n            src = inspect.getsource(mod)\n            h.update(module_name.encode())\n            h.update(b\"\\x00\")\n            h.update(src.encode())\n            h.update(b\"\\x00\")\n        except (ModuleNotFoundError, OSError, TypeError):\n            # Module absent OR source unavailable → contribute nothing for this slot\n            continue\n    return h.digest()\n\n\n# --------------------------------------------------------------------------- #\n# Adapters that plug into CALT's ChainLoadPreprocessor                         #\n# --------------------------------------------------------------------------- #\n\nclass RawLineToTupleAdapter:\n    \"\"\"\n    Splits a raw `\"input # target\"` line into `(input, target)`.\n\n    Used as the FIRST step of the chain when a task has no other load\n    preprocessor (e.g. groebner degrevlex+raw) but the user wired hooks.\n    CALT's chain expects the first preprocessor to handle the raw source.\n\n    Same parsing as `calt.io.read.read_data_from_file` (single `#` split).\n    \"\"\"\n\n    def process_sample(self, source):\n        if isinstance(source, str):\n            line = source.strip()\n            if \"#\" not in line:\n                raise ValueError(\n                    f\"Raw line must contain '#' separating input and target: {line[:80]!r}...\"\n                )\n            input_part, target_part = line.split(\"#\", 1)\n            return input_part.strip(), target_part.strip()\n        # Already a tuple/dict from an earlier preprocessor — pass through unchanged.\n        return source\n\n\nclass UserPostProcessorAdapter:\n    \"\"\"\n    Wraps user hooks into a CALT-compatible LoadPreprocessor.\n\n    Receives `(input_text, target_text)` from the previous step, applies each\n    user function in order, returns `(input_text, target_text)`.\n    \"\"\"\n\n    def __init__(self, hooks: list[PostFn]):\n        if not hooks:\n            raise ValueError(\"UserPostProcessorAdapter requires at least one hook\")\n        self.hooks = list(hooks)\n\n    def process_sample(self, source):\n        if isinstance(source, tuple) and len(source) == 2:\n            inp, tgt = source\n        elif isinstance(source, dict):\n            # Should be unusual at this stage; convert to tuple for hooks.\n            inp = source.get(\"problem\")\n            tgt = source.get(\"answer\", source.get(\"solution\"))\n        else:\n            raise TypeError(\n                f\"UserPostProcessorAdapter expects (str, str) tuple or dict, got {type(source).__name__}\"\n            )\n        for fn in self.hooks:\n            inp, tgt = fn(inp, tgt)\n        return inp, tgt\n\n\n__all__ = [\n    \"PostFn\",\n    \"get_user_postprocessors\",\n    \"user_postprocessor_hash_contribution\",\n    \"RawLineToTupleAdapter\",\n    \"UserPostProcessorAdapter\",\n]\n","pyproject.toml":"[project]\nname = \"calt-experiments\"\nversion = \"0.1.0\"\ndescription = \"Experiments for learning algebraic computations with Transformers\"\nrequires-python = \">=3.10,<3.13\"\ndependencies = [\n    # calt-x 1.3.0 (PyPI). Provides calt.io.preprocess, the encoder-only model and\n    # pluggable embeddings that the tasks rely on (PR #36 + PR #38).\n    \"calt-x==1.3.0\",\n    \"omegaconf>=2.3.0\",\n    \"matplotlib>=3.7.0\",\n    \"click>=8.0.0\",\n]\n\n[tool.ruff]\nline-length = 100\n","CALT_SNAPSHOT.txt":"CALT project snapshot\n=====================\nBundled on : 2026-07-24\nEngine     : calt-x 1.3.0 (PyPI). Provides calt.io.preprocess\n\nThese task files were captured by the CALT Task Builder on the date above.\nIf a task stops working after a calt-x release, check that the installed\ncalt-x matches the engine version listed here.\n"};
